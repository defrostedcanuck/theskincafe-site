---
phase: 01-client-infrastructure-provisioning
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md
  - docs/env/README.md
  - .vercel/project.json
autonomous: false
requirements:
  - INFRA-01
user_setup:
  - service: vercel
    why: "Project must run from client-owned Vercel team (INFRA-01); client is account owner."
    env_vars: []
    dashboard_config:
      - task: "Create Vercel team (client logs into vercel.com, clicks 'Create Team', picks team slug)"
        location: "https://vercel.com/teams/create"
      - task: "Invite craig@neuralrebel.ai as Admin/Owner-equivalent"
        location: "Client Team → Settings → Members → Invite"
      - task: "Attach a valid payment method to the client team BEFORE transfer initiation"
        location: "Client Team → Settings → Billing"
must_haves:
  truths:
    - "The Vercel project previously under team_SgLdPAO4LBFQUJZ4x4VoMGLo (Neural Rebel) is owned by the client's Vercel team"
    - "A `git push` to main origin triggers a deploy in the client Vercel team's project, not the Neural Rebel team"
    - "Local `.vercel/project.json` points to the client team's orgId and projectId (not Neural Rebel's)"
    - "Environment variables present before transfer are still present after transfer under client team Project Settings → Environment Variables"
    - "`docs/env/README.md` exists as the catalog of all env vars Phase 1 will seed, documenting names/scopes without committing values"
  artifacts:
    - path: ".planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md"
      provides: "Transfer decision record: pre-transfer checklist verification, transfer timestamp, post-transfer re-link steps, manual redeploy proof"
      contains: "vercel teams ls"
    - path: "docs/env/README.md"
      provides: "Env var catalog (names + scope + source) — committed; values are NOT committed"
      contains: "RESEND_API_KEY"
    - path: ".vercel/project.json"
      provides: "Local link to client-team project (regenerated after transfer)"
  key_links:
    - from: "git push origin main"
      to: "Client Vercel team project → auto-deploy"
      via: "Vercel GitHub App installed on defrostedcanuck org OR client's org (Option A default: stays on defrostedcanuck)"
      pattern: "Deployed.*client-team"
    - from: "docs/env/README.md"
      to: "Plans 04 + 05 env var seeding"
      via: "single source of truth for env var names and scopes"
      pattern: "Scope.*Production.*Preview"
---

<objective>
Transfer the existing `theskincafe-site` Vercel project from Neural Rebel's team (`team_SgLdPAO4LBFQUJZ4x4VoMGLo`) to the client-owned Vercel team, re-link the local workspace, verify the git-push deploy pipeline still fires from the client team, and stand up the `docs/env/` env-var catalog so Plans 04 and 05 have a documented target schema to seed into.

Purpose: Satisfies INFRA-01 (site deployed on client-owned Vercel team). The transfer path is explicitly chosen over "create fresh project on client team" per RESEARCH.md Anti-Patterns — fresh creation loses deploy history, preview URLs, and git integration state.

Output: Project transferred, local link regenerated, redeploy verified, transfer artifact committed, env-var catalog committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-client-infrastructure-provisioning/01-RESEARCH.md
@.planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md
@AGENTS.md
@.vercel/project.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Wait for client Vercel team invite + billing setup, verify access</name>
  <files>.planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/01-client-infrastructure-provisioning/01-RESEARCH.md (section: "Vercel Project Transfer Reference > Prerequisites")
    - .planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md (rows #1 and #2 under Client-Blocking)
  </read_first>
  <what-built>
    This is a client-action gate. Plan 01 produced the checklist identifying client-blocking rows #1 (Vercel team creation + invite craig@neuralrebel.ai) and #2 (valid payment method on client team). This checkpoint confirms those are both complete before we initiate the transfer, because RESEARCH.md pitfall-analysis shows the #1 common transfer failure is "target team has no valid payment method." Without billing attached, the transfer API call rejects.
  </what-built>
  <how-to-verify>
    Craig confirms ALL of the following. If ANY fails, pause and surface to the user:

    1. Run `vercel teams ls` in the project directory. Output MUST show the client's team name (not only Neural Rebel). Capture the exact slug in the checklist row #1 "Status" column.

    2. Switch to client team: `vercel switch <client-team-slug>`. Expected output: `Success! The team <client-team-slug> is now active!`

    3. Verify payment method: open `https://vercel.com/teams/<client-team-slug>/settings/billing` in a browser. Confirm "Payment Method" section shows an active card/bank, not "No payment method on file." (Vercel dashboard, not CLI — billing info is not exposed via CLI.)

    4. Update `.planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md`:
       - Check off rows #1 and #2 in Client-Blocking section.
       - Fill "Status" column with timestamped confirmation (e.g., `✅ 2026-04-XX — team slug: skincafe; billing: Visa ending 1234`).

    If `vercel teams ls` does not include the client team, the invitation was not accepted yet — DO NOT proceed. Surface to user: "Awaiting client to invite craig@neuralrebel.ai to Vercel team <team-name>." If `vercel` CLI is not installed, run `npm i -g vercel@latest` first per RESEARCH.md "Environment Availability."
  </how-to-verify>
  <resume-signal>Type "approved" after `vercel teams ls` shows client team AND billing is attached, and the checklist is updated. Type "blocked — waiting on <specific item>" otherwise.</resume-signal>
  <action>Pause execution until Craig verifies the client has created their Vercel team, invited craig@neuralrebel.ai as Admin, and attached a valid payment method. Perform the three verification steps described in <how-to-verify>, then update HANDOFF-CUTOVER-CHECKLIST.md Client-Blocking rows #1 and #2. Resume when user types 'approved'.</action>
  <verify>
    <automated>vercel teams ls 2>&1 | grep -qi 'skin\|cafe\|client' || echo 'Awaiting client-team-slug confirmation in checklist'</automated>
  </verify>
  <done>Client Vercel team exists, Craig is a member (confirmed via `vercel teams ls`), billing is attached, and HANDOFF-CUTOVER-CHECKLIST.md rows #1 + #2 are marked complete with timestamp.</done>
</task>

<task type="auto">
  <name>Task 2: Transfer Vercel project + re-link local workspace + verify deploy pipeline</name>
  <files>
    .vercel/project.json,
    .planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md,
    .planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md
  </files>
  <read_first>
    - .planning/phases/01-client-infrastructure-provisioning/01-RESEARCH.md (entire "Vercel Project Transfer Reference" section — preserved vs not-preserved lists, post-transfer tasks)
    - .planning/phases/01-client-infrastructure-provisioning/01-RESEARCH.md (Pitfall 4: "Vercel integrations don't transfer")
    - .vercel/project.json (current state: orgId = team_SgLdPAO4LBFQUJZ4x4VoMGLo)
  </read_first>
  <action>
    The transfer is a dashboard-driven operation. Vercel CLI does not expose `vercel project transfer`, so this task is a sequence of (a) UI-driven transfer, (b) CLI-driven re-link + verification.

    Step 1 — Snapshot pre-transfer state (for transfer artifact):
    ```bash
    cat .vercel/project.json
    vercel teams ls
    vercel env ls --environment production
    vercel env ls --environment preview
    ```
    Capture each command's output verbatim in `.planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md` under a "Pre-Transfer Snapshot" heading with timestamp.

    Step 2 — Initiate transfer via dashboard:
    1. Open `https://vercel.com/defrostedcanuck/theskincafe-site/settings/general` (or wherever the project currently lives under Neural Rebel; `.vercel/project.json` shows orgId `team_SgLdPAO4LBFQUJZ4x4VoMGLo`).
    2. Scroll to "Transfer Project" section (near bottom of General settings).
    3. Click "Transfer". Dialog asks for target team — select the client team slug recorded from Task 1.
    4. Confirm transfer. Wait for completion (10s–10min per RESEARCH.md).

    If the dashboard rejects the transfer with "target team has no valid payment method," abort and return to Task 1 checkpoint with that error — do NOT retry; escalate to user.

    Step 3 — Re-link local workspace:
    ```bash
    rm -f .vercel/project.json    # Stale — points at Neural Rebel
    vercel link --yes    # Interactive: pick client team, pick theskincafe-site project
    ```
    If `vercel link --yes` cannot auto-pick (multiple projects, ambiguous), run `vercel link` without `--yes` and follow prompts: choose client scope, choose `theskincafe-site` project.

    After re-link, verify:
    ```bash
    cat .vercel/project.json
    # Expected: orgId is NOT team_SgLdPAO4LBFQUJZ4x4VoMGLo; projectId unchanged
    ```
    Record new orgId in VERCEL-TRANSFER.md.

    Step 4 — Verify env vars carried over:
    ```bash
    vercel env ls --environment production
    vercel env ls --environment preview
    ```
    Diff against pre-transfer snapshot in Step 1. All env vars present pre-transfer MUST be present post-transfer (per RESEARCH.md "Preserved on transfer" list). Record result in artifact.

    If the project had no env vars pre-transfer (likely — RESEARCH.md Runtime State Inventory line "Vercel project currently has no env vars set"), note that and proceed.

    Step 5 — Verify git integration still fires:
    ```bash
    git log -1 --oneline    # Capture current HEAD
    git commit --allow-empty -m "chore(phase-01): trigger deploy post-Vercel-team-transfer"
    git push origin main
    ```
    Then poll:
    ```bash
    vercel ls theskincafe-site --scope <client-team-slug>
    ```
    Confirm a new deployment appears with timestamp matching the push. Record the deployment URL in VERCEL-TRANSFER.md.

    If no deploy fires within 2 minutes, RESEARCH.md Pitfall 4 triggered — GitHub integration broke. Remediate: open `https://vercel.com/<client-team-slug>/theskincafe-site/settings/git`, re-link repository. Reinstall Vercel GitHub App on `defrostedcanuck` org if prompted. Re-trigger empty commit.

    Step 6 — Update handoff checklist:
    In `.planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md`:
    - Mark plan 02 status as "Complete" in the status table.
    - Add row to Client-Blocking row #1 "How to verify" column referencing the VERCEL-TRANSFER.md proof.

    Transfer artifact content (VERCEL-TRANSFER.md) must include:
    - Pre-transfer snapshot (Step 1 output).
    - Transfer timestamp + who initiated.
    - Post-transfer `.vercel/project.json` contents (orgId + projectId).
    - Env var carryover diff.
    - Post-transfer empty-commit deploy URL + status.
    - Any recovery steps taken (e.g., GitHub App reinstall) noted verbatim.
  </action>
  <acceptance_criteria>
    - `.vercel/project.json` orgId is NOT `team_SgLdPAO4LBFQUJZ4x4VoMGLo`: `grep -q "team_SgLdPAO4LBFQUJZ4x4VoMGLo" .vercel/project.json` returns NON-ZERO (i.e., NOT found; grep exits 1).
    - `.vercel/project.json` still has the original projectId (verify pre/post snapshot match on projectId field).
    - `vercel teams ls` shows current scope is the client team.
    - VERCEL-TRANSFER.md exists: `test -f .planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md`
    - VERCEL-TRANSFER.md contains pre-transfer snapshot: `grep -q "Pre-Transfer Snapshot" .planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md`
    - VERCEL-TRANSFER.md contains post-transfer deploy URL: `grep -Eq "https://.*vercel\.app" .planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md`
    - Empty-commit deploy fired successfully: `vercel ls theskincafe-site --scope <client-team-slug>` shows a deployment created after the empty-commit push.
    - No credentials committed: `git log --all -p -- .vercel/` shows only orgId/projectId (both are non-secret per Vercel docs; projectId + orgId are meant to be committed).
    - HANDOFF-CUTOVER-CHECKLIST.md status table row for plan 02 reads "Complete": `grep -A20 "^## .*Status" .planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md | grep -E "02.*(Complete|✅)"`
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "team_SgLdPAO4LBFQUJZ4x4VoMGLo" .vercel/project.json && test -f .planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md && grep -q "Pre-Transfer Snapshot" .planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md && grep -Eq "https://.*vercel\.app" .planning/phases/01-client-infrastructure-provisioning/artifacts/VERCEL-TRANSFER.md</automated>
  </verify>
  <done>Project lives under client Vercel team, local workspace re-linked, `git push origin main` deploys to client team project. Transfer audit trail committed. Any integration recovery steps (GitHub App reinstall) documented.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold docs/env/ catalog (env var schema, not values)</name>
  <files>docs/env/README.md</files>
  <read_first>
    - .planning/phases/01-client-infrastructure-provisioning/01-RESEARCH.md (section: "Environment Variables to Provision (Vercel)")
    - .planning/phases/01-client-infrastructure-provisioning/01-RESEARCH.md (Credential Storage Pattern — Vercel is source of truth, `.env.local` is scratch, never commit values)
    - .gitignore (verify `.env*` is ignored before creating any doc that might reference values)
  </read_first>
  <action>
    This task documents env var NAMES and SCOPES only. Values are seeded by Plans 04 and 05 directly into Vercel. `docs/env/README.md` is the schema catalog — it's safe to commit because it holds only names, scopes, and "where to get the value" pointers.

    First, verify gitignore safety:
    ```bash
    grep -E "^\.env" .gitignore
    ```
    MUST return a match. If `.gitignore` does not ignore `.env*`, STOP and remediate by adding `.env*` to `.gitignore` before any value ever lands locally. (Note: additional_context rsync-clobber memory makes this critical.)

    Create `docs/env/README.md` with this structure, populated from RESEARCH.md's "Environment Variables to Provision (Vercel)" table verbatim:

    # Environment Variables — The Skin Cafe

    Source of truth: **Vercel project Environment Variables** under the client Vercel team. This file documents what variables exist, their scope, and how to obtain values. Values themselves are NEVER committed here or anywhere in the repo. Local development uses `.env.local` (gitignored; sandbox/dev values only).

    ## Variable Catalog

    | Env Var | Scope | Source (Dev) | Source (Production + Preview) | Seeded by Plan |
    |---------|-------|--------------|-------------------------------|----------------|
    | `RESEND_API_KEY` | Production, Preview | `re_test_...` dev key from Resend dashboard | `re_...` production key restricted to verified `theskincafe.net` sending domain | Plan 04 |
    | `STAFF_NOTIFICATION_EMAIL` | Production, Preview, Development | placeholder during build (e.g. `hello@theskincafe.net`) | client-confirmed staff address (open question #6 in HANDOFF-CUTOVER-CHECKLIST.md) | Plan 04 |
    | `SQUARE_ACCESS_TOKEN` | Production, Preview | Square **sandbox** access token | Square **production** personal access token (CUSTOMERS_WRITE + CUSTOMERS_READ scope) | Plan 05 |
    | `SQUARE_API_BASE` | Production, Preview, Development | `https://connect.squareupsandbox.com` | `https://connect.squareup.com` | Plan 05 |
    | `SQUARE_APPLICATION_ID` | Production, Preview | sandbox application id | production application id | Plan 05 |
    | `SQUARE_LOCATION_ID` | Production, Preview | optional — default sandbox location | primary merchant location (Gilbert OR Scottsdale — client to specify) | Plan 05 |
    | `NEXT_PUBLIC_SITE_URL` | Production, Preview, Development | `http://localhost:3000` | `https://www.theskincafe.net` (or client-confirmed canonical) | Plan 02 follow-up or Plan 05 |

    ## Scoping Rules

    - `Production` + `Preview` are separate scopes in Vercel. Preview deploys hit SANDBOX Square per the locked decision in additional_context (to avoid polluting the client's live Square Marketing list during PR testing).
    - `Development` scope is only used when `vercel env pull` is run locally; otherwise prefer a hand-maintained `.env.local`.
    - `NEXT_PUBLIC_*` prefix exposes to browser. Only `NEXT_PUBLIC_SITE_URL` uses it. Square and Resend keys are server-only.

    ## Rotation Procedure

    Placeholder — full procedure belongs in Phase 4 handoff doc (INFRA-06). For Phase 1, record:
    - Resend API key: rotate in Resend dashboard → Vercel env update → redeploy.
    - Square access token: rotate in Square Developer Dashboard → Vercel env update → redeploy.

    ## Anti-Patterns (do not do)

    - Do NOT commit `.env*` files. Verified: `.gitignore` ignores `.env*`.
    - Do NOT store production credentials in `.env.local` — rsync clobber risk (see global CLAUDE.md).
    - Do NOT reuse one Square token for dev + prod — sandbox vs production separation is locked.
    - Do NOT prefix server-only secrets with `NEXT_PUBLIC_`.

    Update HANDOFF-CUTOVER-CHECKLIST.md Craig-Unblocked section to check off "Scaffold docs/env/ directory and README.md."

    Do NOT create files under `docs/env/` with actual values. Plans 04 and 05 seed Vercel env directly via `vercel env add`, not via committed files.
  </action>
  <acceptance_criteria>
    - `docs/env/README.md` exists: `test -f docs/env/README.md`
    - File catalogs all 7 env vars from RESEARCH.md (each name must appear; backticks around the name are optional so executor formatting is tolerated):
      - `grep -q "RESEND_API_KEY" docs/env/README.md`
      - `grep -q "STAFF_NOTIFICATION_EMAIL" docs/env/README.md`
      - `grep -q "SQUARE_ACCESS_TOKEN" docs/env/README.md`
      - `grep -q "SQUARE_API_BASE" docs/env/README.md`
      - `grep -q "SQUARE_APPLICATION_ID" docs/env/README.md`
      - `grep -q "SQUARE_LOCATION_ID" docs/env/README.md`
      - `grep -q "NEXT_PUBLIC_SITE_URL" docs/env/README.md`
    - File calls out source of truth: `grep -q "source of truth" docs/env/README.md` (case-insensitive)
    - File does NOT contain actual credential values: `! grep -Eiq "re_(test_|[A-Za-z0-9]{20})" docs/env/README.md && ! grep -Eq "sq0[a-z]tp-[A-Za-z0-9_-]+" docs/env/README.md` (no Resend or Square token patterns)
    - `.gitignore` excludes `.env*`: `grep -q "\\.env" .gitignore`
    - No `.env.local` or `.env.production` committed: `git ls-files | grep -E "^\\.env" | wc -l` returns `0`
  </acceptance_criteria>
  <verify>
    <automated>test -f docs/env/README.md && grep -q "RESEND_API_KEY" docs/env/README.md && grep -q "STAFF_NOTIFICATION_EMAIL" docs/env/README.md && grep -q "SQUARE_ACCESS_TOKEN" docs/env/README.md && grep -q "SQUARE_API_BASE" docs/env/README.md && grep -q "SQUARE_APPLICATION_ID" docs/env/README.md && grep -q "SQUARE_LOCATION_ID" docs/env/README.md && grep -q "NEXT_PUBLIC_SITE_URL" docs/env/README.md && ! grep -Eiq 're_(test_|[A-Za-z0-9]{20})' docs/env/README.md && grep -q "\.env" .gitignore</automated>
  </verify>
  <done>`docs/env/README.md` committed with all 7 env vars cataloged by name, scope, source, and seeding plan. No credential values anywhere. `.gitignore` verified safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Neural Rebel Vercel team → client Vercel team | Project + env vars cross agency → client boundary; integrations do NOT cross |
| Vercel env store → GitHub Actions / local CI | Production secrets available to any deployment in client team |
| Developer laptop → git | `.vercel/project.json` commits to repo; must NOT contain credentials |
| docs/env/README.md → git | Schema doc; must never leak actual values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | Env var value accidentally committed to docs/env/README.md or VERCEL-TRANSFER.md | mitigate | Task 3 acceptance_criteria greps for known credential formats (`re_...`, `sq0atp-...`) and fails build if found. Task 2 artifact captures only `vercel env ls` (which shows names + scopes, NOT values) — `vercel env ls` never prints values. |
| T-02-02 | Information Disclosure | `.vercel/project.json` leaks orgId identifying client team publicly | accept | Vercel documents orgId + projectId as safe-to-commit; they are not secrets. Mitigation: none needed. |
| T-02-03 | Tampering | Transfer dashboard UI does not prevent wrong target team selection (e.g., transferring to personal account instead of client team) | mitigate | Task 1 checkpoint captures exact target team slug pre-transfer; Task 2 Step 2 instructs explicit selection of that recorded slug; Step 3 verification reads `.vercel/project.json` orgId to prove correctness. |
| T-02-04 | Denial of Service | GitHub integration breaks post-transfer (Pitfall 4) → `git push` stops deploying → client site drifts from main | mitigate | Task 2 Step 5 explicitly verifies empty-commit redeploy; Step 5 remediation path documented for GitHub App reinstall. |
| T-02-05 | Elevation of Privilege | Preview deploy URLs under client team become accessible to anyone with the URL, potentially before final launch | accept | Vercel preview URLs are unguessable random strings; default security model is sufficient for pre-launch brochure site. Revisit in Phase 4 if client requires password protection (Pro plan feature). |
</threat_model>

<verification>
- `.vercel/project.json` orgId confirms client team ownership.
- `git push origin main` triggered a visible deploy in client Vercel dashboard.
- VERCEL-TRANSFER.md captures the full before/after and the deploy URL as proof.
- `docs/env/README.md` is the documented schema the next two plans will seed against.
- HANDOFF-CUTOVER-CHECKLIST.md plan-02 row reads Complete with timestamped evidence.
- `git log --all -p -- .` shows no credentials anywhere committed.
</verification>

<success_criteria>
- INFRA-01 (client-owned Vercel team) satisfied: project transferred, deploy verified from client team.
- `docs/env/README.md` cataloged, committed, no values leaked.
- Downstream Plans 04 and 05 have a documented env var schema to target.
- Transfer is recoverable: if GitHub App broke, remediation steps are in VERCEL-TRANSFER.md for future reference.
</success_criteria>

<output>
After completion, create `.planning/phases/01-client-infrastructure-provisioning/01-02-SUMMARY.md` with:
- Client Vercel team slug + payment confirmation.
- Pre/post orgId values from `.vercel/project.json`.
- Deploy URL proving post-transfer pipeline works.
- Env var catalog rows added (names only, never values).
- Any recovery steps taken (GitHub App reinstall, etc.).
</output>
