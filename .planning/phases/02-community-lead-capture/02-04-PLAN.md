---
phase: 02-community-lead-capture
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/components/Team.tsx
  - src/app/components/__tests__/Team.test.tsx
autonomous: true
requirements: [COMM-02]
must_haves:
  truths:
    - "Every visible team member's experience string is rendered prominently — visually heavier than the specialty line, legible at 375px without truncation"
    - "Visual treatment handles free-form experience strings (e.g., 'Career aesthetician', 'Since 2003', '16 years as an aesthetician') without appending 'years' or assuming numeric content"
    - "No data changes: all 8 team members, their names, roles, specialties, images, bios, and accepting-clients flags remain identical"
    - "Existing hover-bio overlay, Booked Full badge, and image scale-on-hover transition behave unchanged"
  artifacts:
    - path: "src/app/components/Team.tsx"
      provides: "Team section with raised visual prominence for the experience field"
      contains: "member.experience"
    - path: "src/app/components/__tests__/Team.test.tsx"
      provides: "Smoke test asserting every member's experience string is in the DOM and the visual-prominence class contract holds"
      contains: "expectedExperiences"
  key_links:
    - from: "src/app/components/Team.tsx"
      to: "team member data array"
      via: "team.map((member) => ...)"
      pattern: "team\\.map"
---

<objective>
Raise the visual prominence of each team member's years-in-industry / experience line so the Team section reads as "decades of expertise" at a glance (COMM-02).

Purpose: COMM-02 requires team bios to emphasize "years in industry" as a brand moat for the Skin Cafe's decades-of-care story. The data is already present in `Team.tsx` (every member has an `experience` field); this plan is purely a visual re-weighting — smaller surgical change than a rewrite. RESEARCH.md Anti-Patterns flag that half the team's `experience` values are free-form strings (`"Career aesthetician"`, `"Since 2003"`) not numeric year counts; the visual pattern MUST accommodate free-form copy (no "years" suffix appended, no number formatting assumed).

Output:
- `src/app/components/Team.tsx` edited so the `experience` line renders with a visual pattern that's clearly more prominent than `specialty` but still compositionally balanced within the existing card layout (pill badge with border + champagne accent)
- Smoke test locking data presence + the class contract

**NOT in this plan:** data edits, new photography, layout overhaul. Surgical styling delta only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-community-lead-capture/02-RESEARCH.md
@.planning/phases/02-community-lead-capture/02-01-PLAN.md

# File this plan edits (surgical patch):
@src/app/components/Team.tsx

# Neighboring style language for consistency:
@src/app/components/Services.tsx
@src/app/components/About.tsx
@src/app/globals.css

<current_experience_rendering>
In Team.tsx (line 136), the current rendering is:

    <p className="text-mocha/40 text-[11px] italic">{member.experience}</p>

This is SMALLER and LESS emphasized than `specialty` on line 135 (`text-mocha/50 text-xs`). That's the opposite of what COMM-02 asks for.

The 8 current `experience` values in `team` array:
1. "16 years as an aesthetician"
2. "Nearly 13 years in aesthetics"
3. "Career aesthetician"          -- NOT numeric
4. "Medical background"           -- NOT numeric
5. "Licensed aesthetician"        -- NOT numeric
6. "8 years as a lash artist"
7. "Since 2022"                   -- NOT numeric in the "N years" sense
8. "Since 2003"                   -- same

Any styling MUST work equally well for all eight strings. Do not conditionally format; do not auto-append "years".
</current_experience_rendering>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redesign the experience line as a bordered champagne pill + smoke test</name>
  <files>src/app/components/Team.tsx, src/app/components/__tests__/Team.test.tsx</files>
  <read_first>
    - /Users/craigr/theskincafe-site/src/app/components/Team.tsx (entire file; the 8-member `team` array is data, must not be changed)
    - /Users/craigr/theskincafe-site/src/app/globals.css (verify color tokens `champagne`, `champagne/10`, `espresso`, `mocha` exist; do NOT redefine)
    - /Users/craigr/theskincafe-site/.planning/phases/02-community-lead-capture/02-RESEARCH.md — Anti-Patterns entry "Treating experience: 'Career aesthetician' as a year count" (critical constraint)
  </read_first>
  <behavior>
    - Test 1 (all 8 experience strings render): For each of the 8 known `experience` values, assert the string appears at least once in the rendered DOM
    - Test 2 (prominence class contract): The element containing each `experience` string has the classes `inline-flex`, `bg-champagne/10`, `text-espresso`, `border`, and `font-semibold` — measurably heavier than the `specialty` line which retains `text-mocha/50 text-xs`. Expect exactly 8 such spans
    - Test 3 (no phantom "years" text): The rendered output does NOT include any text injected alongside the experience string. For the members whose `experience` is free-form, the rendered text equals EXACTLY the data value (no "years" suffix, no prefix)
    - Test 4 (accepting-clients badge preserved): Tasha Scott (acceptingClients: false) still renders with a "Booked Full" badge
    - Test 5 (member count): Exactly 8 h3 team-member-name headings render
  </behavior>
  <action>
    1. In `src/app/components/Team.tsx`, modify ONLY the info block inside `team.map(...)` (the `<div className="p-4">` sub-tree, current lines ~128–137). Keep every other JSX element identical: the photo container, the aspect-ratio wrapper, the hover bio overlay, the Booked Full badge, the social link corner button, the ScrollReveal wrapper, the outer card Tailwind classes.

    2. Restructure the three existing `<p>` tags inside `<div className="p-4">` so the visual hierarchy becomes:
       - Name (h3) — unchanged
       - Role eyebrow (existing `text-champagne text-xs font-medium uppercase tracking-wider`) — unchanged
       - Experience pill (NEW prominent treatment — replaces the current `text-mocha/40 text-[11px] italic` line)
       - Specialty (demoted to a secondary line beneath the pill — `text-mocha/50 text-xs`)

       The pill markup:

           <span className="inline-flex items-center gap-1.5 mt-2 mb-2 px-2.5 py-1 rounded-full bg-champagne/10 border border-champagne/30 text-espresso text-[11px] font-semibold tracking-wide">
             {member.experience}
           </span>

       Rationale for each class:
       - `inline-flex` hugs content — works for "Career aesthetician" AND "16 years as an aesthetician"
       - `bg-champagne/10 border border-champagne/30` = subtle branded pill; champagne is the site's primary accent
       - `text-espresso font-semibold` = weightier than `text-mocha/50` specialty below
       - `text-[11px] tracking-wide` keeps the badge compact; the pill shape conveys "this matters"

       Final info block template:

           <div className="p-4">
             <h3 className="font-[var(--font-display)] text-lg font-semibold text-espresso">
               {member.name}
             </h3>
             <p className="text-champagne text-xs font-medium uppercase tracking-wider mb-1">
               {member.role}
             </p>
             <span className="inline-flex items-center gap-1.5 mt-2 mb-2 px-2.5 py-1 rounded-full bg-champagne/10 border border-champagne/30 text-espresso text-[11px] font-semibold tracking-wide">
               {member.experience}
             </span>
             <p className="text-mocha/50 text-xs">{member.specialty}</p>
           </div>

       Ordering rationale: experience goes above specialty because "years in industry" is the trust signal; specialty is secondary context. Eye lands on name → role eyebrow → pill (trust) → specialty (detail).

    3. Do NOT change:
       - The `team` data array (all 8 members, field values untouched)
       - The photo markup, sizes, aspect ratio, hover overlay, scale animation
       - The `acceptingClients` Booked Full badge rendering
       - The Camera social icon corner button
       - The outer card/container Tailwind classes
       - The section title "Meet the Experts", eyebrow "Our Artists", or intro copy

    4. Create `src/app/components/__tests__/Team.test.tsx` with these tests (pseudocode shown; use Vitest + RTL):

       - Import `{ describe, it, expect }` from 'vitest', `{ render, screen }` from '@testing-library/react', and `Team` from '@/app/components/Team'
       - Define `expectedExperiences` array of all 8 literal strings from the current `team` data
       - Test 1: render Team, assert `screen.getAllByRole('heading', { level: 3 })` has length 8
       - Test 2: for each string in `expectedExperiences`, assert `screen.getAllByText(exp).length >= 1`
       - Test 3: query `container.querySelectorAll('span')`, filter by className containing `bg-champagne/10`, `border`, `text-espresso`, `font-semibold`, assert filtered length === 8
       - Test 4: assert `container.textContent` contains "Career aesthetician" but NOT "Career aesthetician years"; same for "Since 2003"
       - Test 5: `expect(screen.getByText(/booked full/i)).toBeInTheDocument()`

       Note for Test 3: Tailwind v4 preserves class names literally in the `className` string attribute on the DOM node (it's only in the compiled CSS that escaping matters). So `cls.includes('bg-champagne/10')` on the className string works.

    DO NOT use `framer-motion` here. DO NOT pull the `team` array out into a new file (keep diff surgical). DO NOT change image sizes, the `sizes=` prop values, or the aspect ratio. DO NOT remove the `italic` styling anywhere the file uses it outside the experience line.
  </action>
  <verify>
    (The automated block lives below in <verification>. Keeping task-level verify simple to avoid XML nesting collisions.)
    Run: `npm run test -- src/app/components/__tests__/Team.test.tsx`
    Expected: all 5 tests pass.
  </verify>
  <done>
    - `src/app/components/Team.tsx` `<div className="p-4">` block now contains a `<span>` rendering `{member.experience}` with classes `inline-flex`, `bg-champagne/10`, `border`, `border-champagne/30`, `text-espresso`, `font-semibold`
    - The old `<p className="text-mocha/40 text-[11px] italic">{member.experience}</p>` is gone
    - The `{member.specialty}` line still exists using `text-mocha/50 text-xs`
    - The `team` array (lines 5–78) is byte-identical to the pre-edit version
    - The Camera corner button block is byte-identical
    - The acceptingClients Booked Full badge block is byte-identical
    - All 5 Team tests pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| data array → DOM render | `team` array is authored by the dev (trusted); rendered text is not user input |
| CSS class string → browser | No user-controlled interpolation into classNames |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-23 | I (Info Disclosure) | Team.tsx renders | accept | All team data is intentionally public (names, roles, specialties, bios). No PII beyond what's already on the live site. |
| T-02-24 | T (Tampering — rendered text) | experience field | mitigate | Test 3 locks "no phantom suffix" invariant. If a future refactor adds `${exp} years` logic, the test fails. |
| T-02-25 | A (Availability — visual regression) | Team section layout | accept | Pure CSS change; rollback is `git revert`. No runtime cost. |
</threat_model>

<verification>
Automated checks (run from repo root):

- `npm run test -- src/app/components/__tests__/Team.test.tsx` exits 0 with 5 passing tests
- `grep -q "bg-champagne/10" src/app/components/Team.tsx` exits 0
- `grep -q "border-champagne/30" src/app/components/Team.tsx` exits 0
- `grep -q "{member.experience}" src/app/components/Team.tsx` exits 0
- `grep -c "{member.experience}" src/app/components/Team.tsx` returns 1 (one render site, not duplicated)
- `! grep -q "text-mocha/40 text-\\[11px\\] italic" src/app/components/Team.tsx` exits 0 (old styling removed)
- `! grep -q "years\"[^\"]*+.*experience\\|experience.*+.*\" years\"" src/app/components/Team.tsx` exits 0 (no suffix injection pattern added)
- `npm run build` succeeds (Tailwind v4 compiles the arbitrary-value classes; no type errors)
</verification>

<success_criteria>
1. All 8 team members render with their experience string inside a visually elevated champagne-bordered pill.
2. No data was modified (`git diff src/app/components/Team.tsx` shows only the info-block JSX change).
3. Free-form experience strings ("Career aesthetician", "Since 2003", etc.) render verbatim with no "years" suffix injected.
4. Booked Full badge, hover overlay, image animations, and all other Team section behaviors are visually unchanged.
5. Team tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-community-lead-capture/02-04-SUMMARY.md` documenting: the exact class string used for the pill (for reference by Phase 4 polish/POLISH-02 responsive checks), and any Tailwind v4 gotchas if the arbitrary-value color opacity classes needed adjustment.
</output>
