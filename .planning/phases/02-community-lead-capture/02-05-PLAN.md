---
phase: 02-community-lead-capture
plan: 05
type: execute
wave: 3
depends_on: [02-01, 02-03]
files_modified:
  - src/app/components/Community.tsx
  - src/app/components/Testimonials.tsx
  - src/app/page.tsx
  - src/app/components/__tests__/Community.test.tsx
autonomous: true
requirements: [COMM-01, COMM-03, COMM-04]
must_haves:
  truths:
    - "The homepage renders a new `Community` section that reads as a community showcase — featured quote + avatar row + Instagram CTA tile — distinct from the prior auto-rotating carousel"
    - "The Instagram CTA is a real anchor with `target='_blank' rel='noopener noreferrer'` pointing to the Skin Cafe IG handle, satisfying COMM-03"
    - "Clicking any avatar in the row swaps the featured quote to that customer's testimonial"
    - "The homepage no longer renders the legacy `Testimonials` section (either by removing the import/mount or by deleting the file)"
    - "The homepage renders the BehindTheGlow teaser section from Plan 03 at an appropriate slot so COMM-04 is satisfied end-to-end"
    - "All 9 existing testimonial entries remain usable as data (no content loss)"
  artifacts:
    - path: "src/app/components/Community.tsx"
      provides: "Community section: featured testimonial + avatar wall + Instagram CTA tile (Pattern A from RESEARCH.md)"
      contains: "instagram.com"
    - path: "src/app/page.tsx"
      provides: "Homepage composition updated — Testimonials replaced by Community, BehindTheGlow mounted"
      contains: "Community"
    - path: "src/app/components/__tests__/Community.test.tsx"
      provides: "Tests: featured quote renders, avatar click swaps featured, Instagram CTA has target=_blank + noopener + correct href"
      contains: "rel"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/components/Community.tsx"
      via: "import Community from"
      pattern: "import Community"
    - from: "src/app/page.tsx"
      to: "src/app/components/BehindTheGlow.tsx"
      via: "import BehindTheGlow from"
      pattern: "import BehindTheGlow"
    - from: "src/app/components/Community.tsx"
      to: "https://www.instagram.com/theskincafe"
      via: "anchor tag"
      pattern: "instagram\\.com"
---

<objective>
Replace the homepage's generic testimonial carousel with a "Community" showcase section implementing RESEARCH.md Pattern A (featured quote + avatar wall + Instagram CTA tile), mount the `BehindTheGlow` teaser from Plan 03, and retire the legacy `Testimonials` component (COMM-01, COMM-03; finalizes COMM-04 integration).

Purpose: This plan owns ALL `src/app/page.tsx` composition changes for Phase 2 to avoid file-ownership conflicts with Plans 02/03/04. Plan 03 created `BehindTheGlow.tsx` as a standalone file but did NOT mount it; this plan mounts it. The Testimonials carousel is the anti-pattern COMM-01 explicitly rejects; this plan retires it (deletes the file — RESEARCH.md A7 default recommendation).

Why depends on Plan 03: needs `src/app/components/BehindTheGlow.tsx` to exist before the homepage import compiles. Plan 03 is in Wave 2; this plan is Wave 3 after Plan 03 completes. Plan 02 (Contact) and Plan 04 (Team) are unrelated to page.tsx composition and can run in any wave after Plan 01.

Output:
- `src/app/components/Community.tsx` — new section, Pattern A layout, reuses all 9 existing testimonial entries as data
- `src/app/page.tsx` — updated imports and JSX section order
- `src/app/components/Testimonials.tsx` — DELETED (the file is removed from the repo; `git rm`)
- `src/app/components/__tests__/Community.test.tsx` — tests locking the featured-quote swap, Instagram link safety, avatar count
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-community-lead-capture/02-RESEARCH.md
@.planning/phases/02-community-lead-capture/02-01-PLAN.md
@.planning/phases/02-community-lead-capture/02-03-PLAN.md

# Files this plan touches / references:
@src/app/page.tsx
@src/app/components/Testimonials.tsx
@src/app/components/ScrollReveal.tsx
@src/app/components/About.tsx
@src/app/globals.css

# Plan 03's output that this plan mounts:
@src/app/components/BehindTheGlow.tsx

<testimonial_data>
The 9 existing testimonial entries in `src/app/components/Testimonials.tsx` (lines 8–63) are CONTENT, not just markup. They must be preserved. Copy the `testimonials` array verbatim into `Community.tsx` (or into a new `src/app/data/testimonials.ts` data file — planner discretion). All 9 entries have `{ name, text, service, rating }` shape; `rating` is always 5.

Names (for avatar initials): Jen Jones, Susan Garbayo, Abby Kaufman, Christy Miller, Jane Miller, Michelle B, Hayley Ahearn, Destiny Tanner, Cheri Johnston.
</testimonial_data>

<instagram_handle_decision>
RESEARCH.md Open Question 1 flags: one handle or two? Default from research: single `@theskincafe` (or the actual canonical handle in Team.tsx context). Planner decision for this plan: use a SINGLE link — `https://www.instagram.com/theskincafe` — as the href. If the real handle is different (e.g., `theskincafegilbert`), this is a 1-line string swap when the client confirms. Flag in SUMMARY.md as an item Craig should confirm pre-launch.

Rationale for single CTA now: RESEARCH.md A1 says "planner adds one or two link-outs; layout adapts"; the Pattern A design has room for one CTA tile cleanly, and splitting into two tiles is a visual compromise we'd rather not make without client confirmation.
</instagram_handle_decision>

<page_composition_target>
Current `src/app/page.tsx`:

    Hero → About → Services → Team → Testimonials → Gallery → Locations → BookingCTA → Contact

Target composition after this plan:

    Hero → About → Services → Team → Community → Gallery → Locations → BookingCTA → BehindTheGlow → Contact

Rationale:
- `Community` takes the Testimonials slot (replaces, per RESEARCH.md A7 + Pattern A recommendation)
- `BehindTheGlow` sits between `BookingCTA` and `Contact` — the teaser rides the conversion wave AFTER the primary booking CTA (so we don't compete with booking) and BEFORE the contact form (so it's the "secondary capture" for visitors not ready to book but willing to subscribe). This placement also gives the BehindTheGlow its own quiet slot on the cream bg between two heavier sections.
</page_composition_target>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Community.tsx (Pattern A) with avatar wall + featured quote + Instagram CTA</name>
  <files>src/app/components/Community.tsx, src/app/components/__tests__/Community.test.tsx</files>
  <read_first>
    - /Users/craigr/theskincafe-site/src/app/components/Testimonials.tsx (source the 9-entry `testimonials` array; do NOT import from here — copy)
    - /Users/craigr/theskincafe-site/src/app/components/About.tsx (reference for ScrollReveal usage + section rhythm)
    - /Users/craigr/theskincafe-site/src/app/components/ScrollReveal.tsx (confirm prop shape)
    - /Users/craigr/theskincafe-site/.planning/phases/02-community-lead-capture/02-RESEARCH.md — Community Section Design Patterns → Pattern A (authoritative design brief)
    - /Users/craigr/theskincafe-site/src/app/globals.css (verify tokens: champagne, rose, espresso, sage, cream, mocha, btn-shimmer, gradient-text)
  </read_first>
  <behavior>
    - Test 1 (featured quote default): On initial render, Jen Jones's testimonial (first entry) is the featured quote; the featured text element renders her `text`, and her name + service appear as attribution
    - Test 2 (avatar count): Exactly 9 avatar buttons render in the avatar row — one per testimonial
    - Test 3 (avatar initials): Each avatar displays the member's initials (e.g., "JJ" for Jen Jones, "SG" for Susan Garbayo). Use first-letter-of-first-name + first-letter-of-last-name; for "Michelle B" use "MB"
    - Test 4 (click-to-swap): Clicking the 3rd avatar (Abby Kaufman) replaces the featured quote with Abby's `text` and her attribution
    - Test 5 (Instagram CTA): An anchor with `href="https://www.instagram.com/theskincafe"`, `target="_blank"`, and `rel="noopener noreferrer"` exists in the section (COMM-03). Its visible text or `aria-label` includes "Instagram" or "Follow"
    - Test 6 (semantic section): Renders `<section id="community">` (anchorable)
    - Test 7 (accessibility): Each avatar button has an accessible name (aria-label = "Show testimonial from {member.name}")
    - Test 8 (keyboard usable): Avatars are `<button type="button">` elements (focusable, Enter/Space activates) — NOT `<div onClick>`
  </behavior>
  <action>
    1. Create `src/app/components/Community.tsx` as a client component (state is needed for the active-index). Shape:

       Top of file:

           'use client';

           import { useState } from 'react';
           import ScrollReveal from './ScrollReveal';
           import { Instagram, Quote, Star } from 'lucide-react';

       Data — copy the 9-entry `testimonials` array verbatim from `Testimonials.tsx` (lines 8–63) into this file as a module-level `const testimonials = [ ... ]`. Do NOT import from `Testimonials.tsx` (that file is being deleted in Task 2).

       Helpers:

           const initials = (name: string) =>
             name
               .split(/\s+/)
               .filter(Boolean)
               .slice(0, 2)
               .map((w) => w[0]?.toUpperCase() ?? '')
               .join('');

       Component body (Pattern A layout — shape guide, not prescriptive):

       - `<section id="community" className="py-24 sm:py-32 bg-white relative">`
       - Eyebrow span: `text-champagne ... tracking-[0.2em]` = "Our Community"
       - h2: `font-[var(--font-display)] text-4xl sm:text-5xl font-bold text-espresso` — "Decades of relationships, <span className='gradient-text'>measured in faces we know by name.</span>" (or similar copy aligning with the brand moat from PROJECT.md — the bolded phrase is suggested verbatim from RESEARCH.md Pattern A)
       - section-divider (same utility used by other sections)
       - Featured quote block:
         - Uses `useState<number>(0)` for `activeIndex`; compute `const featured = testimonials[activeIndex]`
         - 5 filled Star icons (champagne tint, matches existing pattern from Testimonials)
         - Large italic pull-quote rendering `featured.text` with decorative `<Quote>` icon
         - Attribution: `{featured.name}` in bold + `{featured.service}` in champagne/80
       - Avatar row:
         - `<div className="flex flex-wrap justify-center gap-3 mt-10">` for a responsive wrap on mobile
         - Map `testimonials.map((t, i) => <button ... />)` — each a circular `w-12 h-12 rounded-full` button
         - Active avatar (i === activeIndex) gets `ring-2 ring-champagne` and `bg-champagne text-white`; others `bg-cream text-espresso border border-latte hover:border-champagne`
         - Button content: `{initials(t.name)}` — 2 letters max, `font-semibold text-sm`
         - `aria-label={`Show testimonial from ${t.name}`}` (accessibility test 7)
         - `onClick={() => setActiveIndex(i)}` swaps the featured
         - After the 9 avatars, render the Instagram CTA tile (Pattern A places it at end of row)
       - Instagram CTA tile (anchor, NOT a button):

             <a
               href="https://www.instagram.com/theskincafe"
               target="_blank"
               rel="noopener noreferrer"
               aria-label="Follow The Skin Cafe on Instagram"
               className="group inline-flex items-center gap-2 px-5 py-3 rounded-full bg-gradient-to-r from-rose/90 to-champagne/90 text-white font-semibold text-sm shadow-sm hover:shadow-lg hover:shadow-rose/20 transition-all hover:-translate-y-0.5"
             >
               <Instagram size={16} aria-hidden="true" />
               Follow on Instagram
             </a>

         Place the anchor just below the avatar row (centered) so it reads as the natural next step after scanning faces. Alternative placement allowed: alongside the last avatar as a "card" tile — planner's visual call. The requirement is that the anchor exists with the tested attributes.

       - Wrap the whole section in a `<ScrollReveal>` for motion consistency with adjacent sections.

       IMPORTANT: keep the component under ~150 lines. If copy/data/helpers inflate it, consider extracting `testimonials` to `src/app/data/testimonials.ts` — planner discretion, but do NOT leave the file >200 lines.

    2. Create `src/app/components/__tests__/Community.test.tsx`:

       - Import `{ describe, it, expect }` from 'vitest', RTL utilities, and `Community` from '@/app/components/Community'.
       - Test 1: render, assert the first testimonial's text is in the document
       - Test 2: `container.querySelectorAll('button[aria-label^="Show testimonial from"]')` length === 9
       - Test 3: assert initials render — for at least 3 known names, use `screen.getByText('JJ')`, `screen.getByText('SG')`, `screen.getByText('MB')`
       - Test 4: click the button labeled "Show testimonial from Abby Kaufman" and assert Abby's text appears (and Jen Jones's text is no longer the active featured one — scope via `within(featuredRegion).queryByText(...)` or simpler: assert Abby's text is visible and the featured region's accessible name/heading updates)
       - Test 5: `const ig = screen.getByRole('link', { name: /instagram|follow/i });` assert `ig.href === 'https://www.instagram.com/theskincafe'` (jsdom resolves href absolute; match with `.endsWith('/theskincafe')` if needed), `ig.target === '_blank'`, `ig.rel` contains both 'noopener' and 'noreferrer'
       - Test 6: `container.querySelector('#community')` is not null
       - Test 7: all 9 buttons have a non-empty `aria-label`
       - Test 8: all avatar-row buttons are `<button>` elements of type "button" (query and assert `.getAttribute('type') === 'button'`)

    DO NOT import the `testimonials` array from `Testimonials.tsx` (it's being deleted). DO NOT embed an Instagram iframe or use the Meta Graph API — link-out only per RESEARCH.md Deferred Ideas. DO NOT add auto-rotate / setInterval to the featured quote — Pattern A is user-driven, not auto-rotating (that was the rejected carousel pattern). Static default is fine.
  </action>
  <verify>
    Run: `npm run test -- src/app/components/__tests__/Community.test.tsx` — expect 8 passing tests.
    Run: `grep -q 'href="https://www.instagram.com/' src/app/components/Community.tsx` exits 0.
    Run: `grep -q 'rel="noopener noreferrer"' src/app/components/Community.tsx` exits 0.
    Run: `grep -q 'target="_blank"' src/app/components/Community.tsx` exits 0.
    Run: `grep -q 'id="community"' src/app/components/Community.tsx` exits 0.
    Run: `grep -q 'setInterval' src/app/components/Community.tsx` — must NOT find (no auto-rotate).
  </verify>
  <done>
    - `src/app/components/Community.tsx` exists, is a `'use client'` file, imports only lucide-react + ScrollReveal + React hooks (no SDK/server imports)
    - Renders 9 clickable avatar buttons with accessible labels
    - Active avatar has distinguishing styles (`ring-2 ring-champagne` + background shift)
    - Instagram anchor has `target="_blank"`, `rel="noopener noreferrer"`, href to the Skin Cafe IG
    - Section has `id="community"`
    - No auto-rotate / setInterval
    - All 8 Community tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Update page.tsx composition and delete Testimonials.tsx</name>
  <files>src/app/page.tsx, src/app/components/Testimonials.tsx</files>
  <read_first>
    - /Users/craigr/theskincafe-site/src/app/page.tsx (full file — current 9 imports + JSX section order)
    - /Users/craigr/theskincafe-site/src/app/components/Testimonials.tsx (confirm the file is isolated: no other components import from it)
    - /Users/craigr/theskincafe-site/.planning/phases/02-community-lead-capture/02-03-PLAN.md (confirms BehindTheGlow.tsx exists with default export from that plan)
  </read_first>
  <action>
    1. Before editing, confirm no other file imports `Testimonials`:

       Run: `grep -r "from ['\"].*Testimonials['\"]" src/` — expect only `src/app/page.tsx` as the match. If any other file imports Testimonials, STOP and surface — deleting would break the build.

    2. Edit `src/app/page.tsx`:

       Remove the line:

           import Testimonials from "./components/Testimonials";

       Add two new imports:

           import Community from "./components/Community";
           import BehindTheGlow from "./components/BehindTheGlow";

       In the `<main>` body, replace:

           <Testimonials />

       with:

           <Community />

       And insert `<BehindTheGlow />` between `<BookingCTA />` and `<Contact />`:

           <BookingCTA />
           <BehindTheGlow />
           <Contact />

       Final section order: Hero → About → Services → Team → Community → Gallery → Locations → BookingCTA → BehindTheGlow → Contact.

    3. Delete `src/app/components/Testimonials.tsx`:

       Run: `git rm src/app/components/Testimonials.tsx` (if the repo is under git; otherwise `rm` the file). The planner's summary should note which was used.

       Do NOT leave the file behind as dead code. The 9 testimonial entries are preserved inside `Community.tsx` (Task 1).

    4. Smoke-verify build: `npm run build` — expect success (no "module not found" for Testimonials, no unused-import errors).

    5. If a Testimonials test file existed at `src/app/components/__tests__/Testimonials.test.tsx` (unlikely — Phase 2 is the first phase writing tests), also delete it.

    DO NOT rearrange unrelated sections (Hero, About, Services, Team, Gallery, Locations, BookingCTA, Contact order/positions stay put — only Testimonials is swapped and BehindTheGlow is inserted). DO NOT inline `<Community />` props to force a specific testimonial — the component manages its own state.
  </action>
  <verify>
    Run: `grep -q "import Community" src/app/page.tsx` exits 0.
    Run: `grep -q "import BehindTheGlow" src/app/page.tsx` exits 0.
    Run: `grep -q "<Community />" src/app/page.tsx` exits 0.
    Run: `grep -q "<BehindTheGlow />" src/app/page.tsx` exits 0.
    Run: `! grep -q "Testimonials" src/app/page.tsx` exits 0 (all references removed).
    Run: `! test -f src/app/components/Testimonials.tsx` exits 0 (file deleted).
    Run: `npm run build` exits 0.
    Run: `npm run test` exits 0 (full suite — all Phase 2 tests pass together).
  </verify>
  <done>
    - `src/app/page.tsx` imports `Community` and `BehindTheGlow`; no longer imports `Testimonials`
    - `<Community />` sits where `<Testimonials />` used to be (between `<Team />` and `<Gallery />`)
    - `<BehindTheGlow />` sits between `<BookingCTA />` and `<Contact />`
    - `src/app/components/Testimonials.tsx` no longer exists in the working tree
    - `npm run build` succeeds with no errors or warnings about missing modules / unused exports
    - Full test suite (`npm run test`) passes — all Phase 2 tests across plans 01–05 green together
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| outbound anchor → instagram.com | Any `target="_blank"` anchor is a reverse-tabnabbing vector without `rel=noopener` |
| client component → module graph | Community.tsx must not transitively pull server-only modules into the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-26 | T (Reverse tabnabbing) | Instagram CTA anchor | mitigate | `rel="noopener noreferrer"` on the anchor; locked by Test 5. Prevents the opened page from accessing `window.opener` or leaking Referer |
| T-02-27 | I (Bundle leak) | Community.tsx | mitigate | Imports limited to `react`, `lucide-react`, and local `ScrollReveal`. No `@/lib/*`, no `square`, no `resend`. Verified by grep in Task 1 verification. Same for BehindTheGlow (Plan 03) — double-checked here because `page.tsx` now mounts both |
| T-02-28 | T (XSS via testimonial text) | featured quote rendering | mitigate | Testimonials are author-controlled data (hard-coded array copied from existing Testimonials.tsx), not user input. React's JSX text escaping handles any special chars naturally. Not using `dangerouslySetInnerHTML` |
| T-02-29 | I (Handle leak — wrong IG account) | Instagram href | accept | If the canonical handle is `theskincafegilbert` instead of `theskincafe`, the link 404s until corrected. One-line fix; flagged in SUMMARY |
| T-02-30 | A (Availability — broken page.tsx import) | homepage build | mitigate | Task 2 Step 1 (grep for other Testimonials imports) + Step 4 (npm run build) prevent a broken build from shipping. If Plan 03 BehindTheGlow.tsx isn't present (Wave ordering), build fails loudly — depends_on: [02-03] enforces this |
| T-02-31 | T (Missing page composition) | BehindTheGlow never mounted | mitigate | This plan's Task 2 explicitly adds `<BehindTheGlow />` between BookingCTA and Contact. Without this step, LEAD-01..04 are unreachable despite being implemented. The `must_haves.truths` entry on homepage mount locks this invariant |
</threat_model>

<verification>
Automated checks (run from repo root):

- `grep -q 'id="community"' src/app/components/Community.tsx` exits 0
- `grep -q 'href="https://www.instagram.com/' src/app/components/Community.tsx` exits 0
- `grep -q 'rel="noopener noreferrer"' src/app/components/Community.tsx` exits 0
- `grep -q 'target="_blank"' src/app/components/Community.tsx` exits 0
- `! grep -q "setInterval\\|useEffect" src/app/components/Community.tsx` (no auto-rotate OR time-based effects)
- `! test -f src/app/components/Testimonials.tsx` (deleted)
- `! grep -r "Testimonials" src/app/` (all references removed)
- `grep -q "import Community" src/app/page.tsx` exits 0
- `grep -q "import BehindTheGlow" src/app/page.tsx` exits 0
- `grep -q "<Community />" src/app/page.tsx` exits 0
- `grep -q "<BehindTheGlow />" src/app/page.tsx` exits 0
- `npm run build` exits 0
- `npm run test` exits 0 (full Phase 2 suite: smoke + square singleton + contact action + Contact component + subscribe action + BehindTheGlow component + Team + Community)
</verification>

<success_criteria>
1. Homepage section order matches target: Hero → About → Services → Team → Community → Gallery → Locations → BookingCTA → BehindTheGlow → Contact.
2. Community section satisfies COMM-01 (distinct from carousel — user-driven quote swap, avatar wall, Instagram tile) and COMM-03 (safe IG link-out).
3. BehindTheGlow section is mounted and reachable, making COMM-04 + LEAD-01..04 functional end-to-end.
4. Testimonials.tsx is fully retired — file deleted, no stale imports, no stale references.
5. All 9 original testimonial entries preserved inside Community.tsx.
6. All Phase 2 plan tests pass together in a single `npm run test` invocation.
7. `npm run build` succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/02-community-lead-capture/02-05-SUMMARY.md` documenting:
- Final page.tsx section order
- Whether the Instagram handle used (`theskincafe`) is confirmed correct — if not, flag as a pre-launch client-confirmation item for Craig
- Whether the Testimonials.tsx delete was via `git rm` or plain `rm`
- Any Pattern A layout deviations from RESEARCH.md
- Any follow-ups for Phase 3 (e.g., analytics custom-event hooks for IG-click, per ANLY-02)
</output>
