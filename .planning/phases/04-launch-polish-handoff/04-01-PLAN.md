---
phase: 04-launch-polish-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/Navbar.tsx
  - public/images/logo-dark.png
autonomous: false
requirements: [POLISH-01]
tags: [polish, navbar, next-image, assets]
user_setup:
  - service: client-deliverable
    why: "Client must supply dark-on-light logo variant — cannot ship POLISH-01 without it"
    dashboard_config:
      - task: "Client delivers dark-on-light logo asset (PNG preferred, 360x130 to match existing logo.png intrinsic dimensions, or SVG if vector)"
        location: "Email/chat deliverable from client"
must_haves:
  truths:
    - "Scrolled navbar shows a native dark-on-light logo (not a CSS invert of the white logo)"
    - "Unscrolled (hero-state) navbar continues to show the existing white logo unchanged"
    - "The word `invert` does not appear in Navbar.tsx"
    - "Logo intrinsic dimensions remain 360x130 (both variants)"
    - "next/image receives a `sizes` hint that matches the rendered logo width (~160px mobile, ~240px desktop)"
  artifacts:
    - path: "public/images/logo-dark.png"
      provides: "Dark-on-light logo variant served by Navbar in scrolled state"
      contains: "binary PNG (or SVG if client delivers that format)"
    - path: "src/app/components/Navbar.tsx"
      provides: "Navbar with scrolled-state logo swap, no invert filter, sizes prop added"
      contains: "logo-dark.png"
  key_links:
    - from: "src/app/components/Navbar.tsx"
      to: "/images/logo-dark.png"
      via: "next/image src={scrolled ? ... : ...}"
      pattern: "logo-dark"
    - from: "src/app/components/Navbar.tsx"
      to: "next/image sizes attribute"
      via: "responsive hint"
      pattern: "sizes="
---

<objective>
Replace the CSS `invert` stopgap in the scrolled-navbar logo with a native dark-on-light logo variant supplied by the client. Add a `sizes` prop to `next/image` for correct responsive serving (free Lighthouse perf win, surfaced in 04-RESEARCH Pitfall 5).

Purpose: Satisfies POLISH-01 locked ROADMAP success criterion #2: "The scrolled-navbar state renders the client-supplied dark-on-light logo variant as a native asset (no CSS `invert` filter remaining anywhere in the Navbar code path)."

Output: Updated `src/app/components/Navbar.tsx` and new `public/images/logo-dark.png` asset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-polish-handoff/04-RESEARCH.md
@src/app/components/Navbar.tsx
@AGENTS.md

<interfaces>
<!-- Current Navbar logo block (lines 44-55 of Navbar.tsx as of 2026-04-20): -->
```tsx
<a href="#" className="flex items-center group">
  <Image
    src="/images/logo.png"
    alt="The Skin Cafe"
    width={360}
    height={130}
    priority
    className={`h-20 w-auto transition-all group-hover:scale-110 ${
      scrolled ? "invert" : ""
    }`}
  />
</a>
```

<!-- Target per 04-RESEARCH Code Examples §"Navbar logo swap (POLISH-01)": -->
```tsx
<a href="#" className="flex items-center group">
  <Image
    src={scrolled ? "/images/logo-dark.png" : "/images/logo.png"}
    alt="The Skin Cafe"
    width={360}
    height={130}
    priority
    sizes="(max-width: 768px) 160px, 240px"
    className="h-20 w-auto transition-all group-hover:scale-110"
  />
</a>
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Confirm client logo asset received</name>
  <what-built>Wave 0 gate — no code change yet. Confirms the dark-on-light logo asset has been received from the client and landed at `public/images/logo-dark.png`.</what-built>
  <how-to-verify>
    1. Check `ls -la public/images/logo-dark.png` — file exists, size > 1KB.
    2. Open the file in Preview.app — it renders as a dark-on-light logo (dark text/mark on a transparent or light background), not a white-on-transparent variant of the existing logo.
    3. Verify intrinsic dimensions: `sips -g pixelWidth -g pixelHeight public/images/logo-dark.png` should return `pixelWidth: 360` and `pixelHeight: 130` (matches existing `logo.png` — per 04-RESEARCH Assumption A8). If the client delivered at different dimensions, note the actual values here; Task 2 will update `width` / `height` props to match.
    4. If the client delivered an SVG instead of PNG: file lives at `public/images/logo-dark.svg` instead; Task 2 will use `<Image unoptimized>` or plain `<img>` (per 04-RESEARCH §"Navbar Logo Swap Pattern" SVG note).
  </how-to-verify>
  <resume-signal>Type "confirmed" once the asset is in place and dimensions noted. If client has not yet delivered, pause the phase and surface as a blocker in STATE.md.</resume-signal>

  <files>N/A — checkpoint task, no file modification</files>
  <action>Human-action checkpoint. Intent: Wave 0 gate — no code change yet. Confirms the dark-on-light logo asset has been received from the client and landed at `public/images/logo-dark.png`. See the <what-built> and <how-to-verify> blocks above for the exact operational steps.</action>
  <verify><automated>echo 'checkpoint gate — see resume-signal for human verification'</automated></verify>
  <done>Checkpoint passed per resume-signal condition above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Swap logo source in Navbar and add sizes prop</name>
  <files>src/app/components/Navbar.tsx</files>
  <read_first>
    - Re-read `src/app/components/Navbar.tsx` lines 44-55 (the Image block) to confirm current state.
    - Re-read 04-RESEARCH.md §"Navbar Logo Swap Pattern" (lines 134-168) for the exact target tsx.
    - AGENTS.md rule: Next.js 16 is not the Next.js in training data. Before saving, skim `node_modules/next/dist/docs/` for any `Image` prop changes in v16 (`priority`, `sizes`, `width`/`height` semantics).
  </read_first>
  <action>
    Edit `src/app/components/Navbar.tsx` lines 44-55. Make two changes to the `<Image>` element:

    1. **Swap src by scrolled state.** Replace `src="/images/logo.png"` with `src={scrolled ? "/images/logo-dark.png" : "/images/logo.png"}`.
    2. **Add sizes prop.** Add `sizes="(max-width: 768px) 160px, 240px"` as a new line below `priority`. Rationale: logo renders at `h-20` (80px tall) → ~160px wide on mobile, ~240px on desktop per Tailwind aspect preservation; without `sizes`, next/image serves the full 360px intrinsic. Source: 04-RESEARCH Pitfall 5 and §"Code Examples".
    3. **Remove the invert hack.** Replace the templated className string `` `h-20 w-auto transition-all group-hover:scale-110 ${scrolled ? "invert" : ""}` `` with the static string `"h-20 w-auto transition-all group-hover:scale-110"`. The `invert` token MUST be gone from the file.

    **If the client delivered SVG instead of PNG** (Task 1 noted this): use `src={scrolled ? "/images/logo-dark.svg" : "/images/logo.png"}` AND add the `unoptimized` prop to the `<Image>` (next/image's optimizer is a no-op on SVG). Do NOT set `dangerouslyAllowSVG` in `next.config.ts` — that's a security surface we don't need.

    **If dimensions differ from 360x130** (Task 1 noted this): update `width` and `height` to the actual intrinsic values from the delivered asset.

    Do NOT:
    - Introduce `next.config.ts` SVG allowlist changes (security scope creep)
    - Add additional className logic for the logo (the scrolled-state color inversion is now asset-driven, not CSS-driven)
    - Remove the `priority` prop (still correct for above-the-fold LCP)
  </action>
  <verify>
    <automated>
      # 1. `invert` is gone from Navbar
      ! grep -n "invert" src/app/components/Navbar.tsx

      # 2. logo-dark source is referenced
      grep -n "logo-dark" src/app/components/Navbar.tsx

      # 3. sizes prop is present
      grep -n 'sizes=' src/app/components/Navbar.tsx

      # 4. next build succeeds (catches any TSX type error from the edit)
      npm run build

      # 5. Local dev smoke: `npm run dev`, open http://localhost:3000, scroll past 50px, inspect `img[alt="The Skin Cafe"]` src attribute in DevTools — should switch between /images/logo.png and /images/logo-dark.png based on scroll state.
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "invert" src/app/components/Navbar.tsx` returns `0`
    - `grep -c "logo-dark" src/app/components/Navbar.tsx` returns at least `1`
    - `grep -c 'sizes="(max-width: 768px)' src/app/components/Navbar.tsx` returns at least `1`
    - `npm run build` exits 0 with no TypeScript errors and no Image-related warnings
    - Manual scroll test in local dev: logo visibly switches from white to dark variant when `scrolled=true` (window.scrollY > 50)
  </acceptance_criteria>
  <done>
    The scrolled navbar renders `logo-dark.png` (or `.svg`) as a native asset. The hero-state navbar still renders the original `logo.png`. The `invert` filter is absent from the entire Navbar component. The `sizes` prop is present on the `next/image` element.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client asset → repo | Unknown-source binary image file; must verify it's the expected logo and not an unintended file swap |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `public/images/logo-dark.png` binary | mitigate | Task 1 (human-action checkpoint) visually verifies the asset renders as the expected dark-on-light Skin Cafe logo before Task 2 references it in code. |
| T-04-01-02 | Information Disclosure | SVG rendering (if client delivers SVG variant) | mitigate | Do NOT set `next.config.ts` `images.dangerouslyAllowSVG: true`. Use `<Image unoptimized>` or treat as static asset. Per 04-RESEARCH §"Navbar Logo Swap Pattern" SVG note. |
| T-04-01-03 | Denial of Service | Missing asset causing 404 + broken image in production | accept | next/image renders a broken-image placeholder but does not crash the app. The Task 2 `npm run build` + dev smoke check catches a bad asset path before commit. |
</threat_model>

<verification>
- `! grep -q "invert" src/app/components/Navbar.tsx` — invert removed
- `grep -q "logo-dark" src/app/components/Navbar.tsx` — new src wired
- `grep -q 'sizes=' src/app/components/Navbar.tsx` — sizes hint added
- `npm run build` exits 0
- Human eyes-on in local dev: scroll trigger produces visible logo swap (not a CSS filter flash)
</verification>

<success_criteria>
- POLISH-01 requirement delivered: dark-on-light logo is a native asset in scrolled state.
- ROADMAP success criterion #2 (Phase 4) satisfied: `invert` is gone from Navbar code path.
- Zero regressions on unscrolled hero-state logo rendering.
- Free performance improvement applied (`sizes` prop) — measurable in Plan 02's Lighthouse baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-polish-handoff/04-01-SUMMARY.md` documenting: the asset file path + format received from client, any dimension changes made to `<Image width/height>`, and a before/after screenshot or rendered-HTML diff if captured.
</output>
