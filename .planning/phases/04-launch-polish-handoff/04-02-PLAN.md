---
phase: 04-launch-polish-handoff
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - tests/responsive.spec.ts
  - package.json
  - .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-baseline-mobile.html
  - .planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md
autonomous: true
requirements: [POLISH-02, POLISH-03]
tags: [responsive, lighthouse, playwright, performance, qa]
must_haves:
  truths:
    - "Playwright responsive harness runs across 375/768/1280 viewports against the current Vercel preview URL"
    - "All 6 documented pages/sections (homepage, services, team, gallery, locations, contact) have zero horizontal scroll at each viewport"
    - "Lighthouse mobile report exists for the preview URL homepage"
    - "Baseline Lighthouse Performance score is captured (pass or fail); if < 90, blockers are enumerated in responsive-qa-notes.md with file references"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright project config with mobile-375, tablet-768, desktop-1280 projects"
      contains: "@playwright/test"
    - path: "tests/responsive.spec.ts"
      provides: "No-horizontal-scroll smoke test across 3 viewports × 6 paths"
      contains: "scrollWidth"
    - path: ".planning/phases/04-launch-polish-handoff/artifacts/lighthouse-baseline-mobile.html"
      provides: "Mobile Lighthouse HTML report against current preview URL"
      min_lines: 1
    - path: ".planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md"
      provides: "Findings doc: per-viewport regressions, Lighthouse perf opportunities, remediation candidates for Plan 03"
      contains: "baseline"
  key_links:
    - from: "tests/responsive.spec.ts"
      to: "theskincafe-site.vercel.app (or current preview URL)"
      via: "page.goto"
      pattern: "vercel.app|theskincafe"
    - from: ".planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md"
      to: "Plan 03 (conditional perf fixes)"
      via: "gap list feeds Plan 03 task scope"
      pattern: "Plan 03|perf"
---

<objective>
Stand up a reproducible responsive + Lighthouse baseline harness and run it against the current Vercel preview URL. Capture the baseline state BEFORE Plan 03's revision round so we know whether perf fixes are needed and whether client revisions land on a stable floor.

Purpose: Satisfies POLISH-02 acceptance (manual walkthrough proof) and POLISH-03 baseline measurement. The final POLISH-03 deliverable (Lighthouse on the production domain) runs in Plan 04 post-cutover — this plan establishes the preview-URL baseline and catches regressions early.

Output: Playwright config + responsive spec (reusable for future regression runs), HTML Lighthouse report committed to the phase artifacts, and a findings doc that lists any layout breaks or perf opportunities worth addressing in Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-launch-polish-handoff/04-RESEARCH.md
@package.json
@AGENTS.md

<interfaces>
<!-- Pages/sections to audit per ROADMAP Phase 4 success criterion #3 (04-RESEARCH §User Constraints) -->
Routes to test:
  - `/` (homepage — hero, services filter, team, gallery, locations, contact)

Section anchors on the homepage (single-page site pattern confirmed by Navbar.tsx navLinks):
  - `/#about`
  - `/#services`
  - `/#team`
  - `/#reviews`
  - `/#gallery`
  - `/#locations`
  - `/#contact`

<!-- Viewports per POLISH-02 -->
  - mobile-375 (375 × 812, iPhone 13 Pro-ish)
  - tablet-768 (768 × 1024, iPad)
  - desktop-1280 (1280 × 800, small laptop)

<!-- Tooling versions verified 2026-04-20 in 04-RESEARCH §Standard Stack -->
  - `@playwright/test@1.59.1`
  - `lighthouse@13.1.0`

<!-- Target for POLISH-03 preview-URL baseline: ≥ 93 (safety margin over the ≥ 90 production target — 04-RESEARCH Pitfall 6) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Install Playwright, add responsive harness + config</name>
  <files>
    package.json
    playwright.config.ts
    tests/responsive.spec.ts
  </files>
  <read_first>
    - `package.json` to confirm no existing `@playwright/test` dependency (04-RESEARCH §Standard Stack notes it is not currently installed).
    - 04-RESEARCH §"Code Examples > Playwright responsive smoke test (POLISH-02)" for the spec template (lines 485-509).
    - 04-RESEARCH §"Alternatives Considered" confirming emulation is right-sized over BrowserStack.
    - Determine current preview URL: run `vercel ls --scope <client-team>` OR check the latest deployment URL in Vercel dashboard. Default assumption per PROJECT.md: `https://theskincafe-site.vercel.app`. If a different preview is canonical, use that.
  </read_first>
  <action>
    Pinned Playwright version per 04-RESEARCH: 1.59.1.

    1. **Install.** `npm install -D @playwright/test@1.59.1` then `npx playwright@1.59.1 install chromium` (only Chromium needed for this pass — mobile Safari quirks are covered by Craig's iPhone smoke in Task 2).

    2. **Create `playwright.config.ts`** at repo root with three projects keyed to the viewports from 04-RESEARCH §"Code Examples". Config contents:

       ```ts
       import { defineConfig } from '@playwright/test';

       export default defineConfig({
         testDir: './tests',
         timeout: 30_000,
         expect: { timeout: 5_000 },
         use: {
           baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'https://theskincafe-site.vercel.app',
           headless: true,
         },
         projects: [
           { name: 'mobile-375', use: { viewport: { width: 375, height: 812 } } },
           { name: 'tablet-768', use: { viewport: { width: 768, height: 1024 } } },
           { name: 'desktop-1280', use: { viewport: { width: 1280, height: 800 } } },
         ],
         reporter: [['html', { outputFolder: '.playwright/report', open: 'never' }]],
       });
       ```

    3. **Create `tests/responsive.spec.ts`** adapted from 04-RESEARCH §"Code Examples". Iterate over seven paths (`/`, `/#about`, `/#services`, `/#team`, `/#reviews`, `/#gallery`, `/#locations`, `/#contact` — eight total since `#about` is also in the nav). For each, assert `document.documentElement.scrollWidth <= document.documentElement.clientWidth + 1` (1px tolerance per research).

       Add a second assertion per viewport: no element overflows beyond the viewport on the x-axis. Use `await expect(page.locator('body')).toHaveCSS('overflow-x', /hidden|clip|visible/)` as a sanity check, and additionally capture a full-page screenshot to `.playwright/screenshots/${project}-${pathSlug}.png` so Task 3 can review them.

    4. **Add npm scripts to `package.json`:**
       - `"test:e2e": "playwright test"`
       - `"test:e2e:report": "playwright show-report .playwright/report"`

    5. **Add `.playwright/` to `.gitignore`** if not present (screenshots and reports are per-run artifacts, not committed).

    Do NOT:
    - Install webkit or firefox browsers (not needed for this pass; emulated Chromium covers the 95% per research).
    - Add Playwright to a CI workflow (scope creep — out-of-scope for Milestone 1).
    - Bake the preview URL as a literal into the spec file; keep it overridable via `PLAYWRIGHT_BASE_URL` env.
  </action>
  <verify>
    <automated>
      # 1. Playwright installed at pinned version
      npm ls @playwright/test | grep -E "@playwright/test@1\.59\."

      # 2. Config file exists and is valid TS
      test -f playwright.config.ts
      npx tsc --noEmit playwright.config.ts 2>&1 | grep -v "error" || true

      # 3. Spec file exists and references three viewport projects
      test -f tests/responsive.spec.ts
      grep -c "mobile-375\|tablet-768\|desktop-1280" tests/responsive.spec.ts
      # Expect: matches (either the viewport names in project config, or the numeric widths referenced in the spec)

      # 4. Dry run lists tests without executing
      npx playwright test --list | head -20
      # Expect: 8 paths × 3 projects = 24 tests listed
    </automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lists `@playwright/test` as a devDependency at `1.59.1` (or compatible patch).
    - `playwright.config.ts` exists at repo root with three viewport projects matching 375/768/1280.
    - `tests/responsive.spec.ts` exists, imports `@playwright/test`, iterates over ≥ 6 paths.
    - `npx playwright test --list` reports ≥ 18 tests (3 projects × ≥ 6 paths).
    - `.gitignore` includes `.playwright/`.
  </acceptance_criteria>
  <done>
    Playwright harness is installable, lists tests cleanly, and the config + spec are ready to run against the preview URL in Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run Lighthouse baseline + responsive sweep against preview URL, capture artifacts</name>
  <files>
    .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-baseline-mobile.html
    .planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md
  </files>
  <read_first>
    - 04-RESEARCH §"Code Examples > Lighthouse CLI run (POLISH-03 deliverable)" — the exact npx invocation (lines 468-481).
    - 04-RESEARCH §"Common Pitfalls" — pitfalls 3 (framer-motion above-fold), 4 (hero video weight), 5 (next/image sizes), 6 (preview-vs-prod delta). Use these as a checklist when annotating findings.
    - Confirm preview URL is reachable: `curl -sI https://theskincafe-site.vercel.app | head -3` (or whatever URL Task 1 resolved).
  </read_first>
  <action>
    1. **Create artifacts directory:** `mkdir -p .planning/phases/04-launch-polish-handoff/artifacts`.

    2. **Run mobile Lighthouse baseline against preview:**

       ```bash
       npx lighthouse@13.1.0 https://theskincafe-site.vercel.app \
         --preset=mobile \
         --output=html \
         --output-path=.planning/phases/04-launch-polish-handoff/artifacts/lighthouse-baseline-mobile.html \
         --chrome-flags="--headless"
       ```

       (Substitute actual preview URL from Task 1 if different.)

       Capture the scores from the JSON summary (Lighthouse prints them to stdout) — record Performance, Accessibility, Best Practices, SEO.

    3. **Run responsive sweep:**

       ```bash
       PLAYWRIGHT_BASE_URL=https://theskincafe-site.vercel.app npx playwright test
       ```

       If any test fails, capture the exact failure output. Save full-page screenshots alongside (the config writes them to `.playwright/screenshots/`).

    4. **Manual iOS Safari smoke** (04-RESEARCH §Alternatives Considered: "Playwright + manual iOS Safari on Craig's phone is right-sized"): open the preview URL on an iPhone and tap through the homepage sections. Log any rendering differences from Chromium emulation. If no iPhone accessible, note "deferred — Craig to run on phone before cutover" in the notes doc; this is acceptable since cutover is gated.

    5. **Write `responsive-qa-notes.md`** with this exact structure:

       ```markdown
       # Phase 4 — Responsive QA + Lighthouse Baseline

       **Preview URL tested:** <actual URL>
       **Run date:** <YYYY-MM-DD>
       **Lighthouse version:** 13.1.0
       **Playwright version:** 1.59.1

       ## Lighthouse (mobile) baseline scores
       | Metric | Score |
       |--------|-------|
       | Performance | XX |
       | Accessibility | XX |
       | Best Practices | XX |
       | SEO | XX |

       **LCP element:** <as reported by Lighthouse>
       **LCP time:** <ms>
       **Largest opportunities** (top 3 from the report's Opportunities panel):
       1. ...
       2. ...
       3. ...

       ## Responsive findings

       ### mobile-375
       - [ ] / — pass / fail (notes)
       - [ ] /#services — ...
       (etc.)

       ### tablet-768
       ...

       ### desktop-1280
       ...

       ## Feeds into Plan 03 (conditional perf fixes)
       - [ ] <Specific file path + action if Performance < 93> — e.g. "src/app/page.tsx hero video: add poster image + preload=metadata per 04-RESEARCH Pitfall 4"
       - [ ] <Specific file path + action if a layout break found> — e.g. "src/app/components/Services.tsx card grid: add `overflow-x-hidden` on mobile"
       - If no fixes needed: note "Baseline passes ≥ 93 at preview; Plan 03 perf-fix tasks skipped."

       ## Manual iOS Safari smoke
       <Results, or "Deferred — Craig to run before Plan 04 cutover">
       ```

    6. **Commit artifacts**: the HTML Lighthouse report and the markdown notes. Do NOT commit `.playwright/` (screenshots stay local).

    Do NOT:
    - Fix anything identified in this task — Plan 03 owns the revision round and conditional perf fixes. Keep this task observation-only.
    - Run Lighthouse against localhost — per 04-RESEARCH Pitfall 6, localhost scores are invalid.
    - Write the production-URL Lighthouse report in this task — that's Plan 04's final deliverable post-cutover.
  </action>
  <verify>
    <automated>
      # 1. Lighthouse HTML exists and is non-trivial
      test -f .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-baseline-mobile.html
      test $(wc -c < .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-baseline-mobile.html) -gt 50000

      # 2. Notes doc exists with required sections
      test -f .planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md
      grep -q "Lighthouse (mobile) baseline scores" .planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md
      grep -q "Responsive findings" .planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md
      grep -q "Feeds into Plan 03" .planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md

      # 3. Lighthouse report HTML references the preview URL
      grep -c "vercel.app\|theskincafe" .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-baseline-mobile.html

      # 4. At least one viewport's sweep result recorded
      grep -c "mobile-375\|tablet-768\|desktop-1280" .planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `lighthouse-baseline-mobile.html` file size > 50KB (real report, not an error page).
    - Notes doc contains the Lighthouse score table populated with actual numbers (not `XX` placeholders).
    - Notes doc lists per-viewport findings for each of the three viewports.
    - Notes doc has a "Feeds into Plan 03" section with either specific remediation items OR an explicit "no fixes needed" note.
    - Preview URL is named in both artifacts (cross-verifiable via grep).
  </acceptance_criteria>
  <done>
    The baseline state of the preview site is measured, documented, and committed. Plan 03 has a concrete, file-path-level gap list to implement against (or a confirmation that no perf fixes are needed).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → preview URL | Outbound HTTP to Vercel preview; no secrets, no write operations |
| Lighthouse headless Chrome → preview URL | Read-only audit traffic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | Lighthouse HTML report committed to repo | accept | Report contains the preview URL (non-sensitive) and performance timings (non-sensitive). No credentials, no PII. Verified by the HTML being the standard Lighthouse template. |
| T-04-02-02 | Tampering | Playwright npm package supply chain | mitigate | Pin to exact version `@playwright/test@1.59.1` per 04-RESEARCH. `npm ci` or lockfile-based installs enforce integrity. |
| T-04-02-03 | Denial of Service | Running full Playwright matrix floods Vercel preview | accept | 24 tests × ~2s each = ~50s total; well within Vercel's free-tier limits. No real user traffic affected. |
| T-04-02-04 | Repudiation | Disputed Lighthouse score in handoff | mitigate | HTML report is committed to git with timestamp + URL baked in. Score is reproducible by re-running the same command. |
</threat_model>

<verification>
- `test -f playwright.config.ts && test -f tests/responsive.spec.ts`
- `npx playwright test --list | wc -l` returns ≥ 18
- Lighthouse HTML exists and is > 50KB
- `responsive-qa-notes.md` exists with populated score table and per-viewport findings
- Notes file contains a concrete "Feeds into Plan 03" action list OR an explicit "no fixes needed" note
</verification>

<success_criteria>
- Reproducible Playwright harness committed (reusable for any future regression pass).
- Mobile Lighthouse baseline captured and committed as HTML artifact.
- POLISH-02 has an automated, scriptable proof mechanism (not just "I eyeballed it").
- Plan 03 has a concrete dependency: either a list of perf fixes to implement, or a sign-off that none are needed.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-polish-handoff/04-02-SUMMARY.md` with: the exact preview URL tested, the four Lighthouse scores, a one-sentence summary of responsive findings, and a bullet list of items handed off to Plan 03 (or a "no handoff needed" note).
</output>
