---
phase: 04-launch-polish-handoff
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md
  - src/app/**/*.tsx
  - src/app/**/*.ts
  - public/**
autonomous: false
requirements: [POLISH-03, POLISH-04]
tags: [revisions, performance, client-gated, scope-control]
user_setup:
  - service: client-action
    why: "Client must submit one bounded round of revisions in writing. Without a written list from the client, POLISH-04 cannot be scoped or closed."
    dashboard_config:
      - task: "Client sends a written list of desired changes (email or chat thread) by the reply-by date specified in the revision-round email."
        location: "Email/chat thread between Craig and client"
must_haves:
  truths:
    - "A written, bounded list of client-requested revisions exists in the repo"
    - "Lighthouse preview-URL Performance is ≥ 93 before Plan 04 cutover (04-RESEARCH Pitfall 6 safety margin), OR the perf gap is explicitly accepted in writing"
    - "Every item on the revision list is either implemented (with file + line references) or explicitly deferred to Milestone 2 with client acknowledgment"
    - "Client has given a written sign-off on the revision round (email, chat screenshot, or checkbox)"
    - "No revision items remain in an ambiguous state at phase end"
  artifacts:
    - path: ".planning/phases/04-launch-polish-handoff/artifacts/revision-round.md"
      provides: "Authoritative record of the revision round: client request, implementation status per item, sign-off"
      contains: "Sign-off"
  key_links:
    - from: ".planning/phases/04-launch-polish-handoff/artifacts/revision-round.md"
      to: "Plan 04 HANDOFF.md appendix"
      via: "revision-round.md is linked or inlined as a handoff appendix"
      pattern: "revision-round"
    - from: ".planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md"
      to: "Plan 03 Task 2 perf fixes"
      via: "Plan 02's 'Feeds into Plan 03' section drives Task 2 scope"
      pattern: "Plan 03"
---

<objective>
Close two client-gated polish items in a single plan:
1. **POLISH-03 preview-URL target (≥ 93):** implement the specific perf fixes identified in Plan 02's `responsive-qa-notes.md` `Feeds into Plan 03` section. Conditional — if Plan 02's baseline already passes ≥ 93, Task 2 is a no-op.
2. **POLISH-04:** execute exactly one bounded round of client revisions, captured in writing from kickoff to sign-off, with scope-creep controls per 04-RESEARCH Pitfall 7.

**Scope boundary — locked:** This plan implements **one round only.** Any revision request received after the reply-by date is logged and deferred to Milestone 2. This is the 04-RESEARCH Pitfall 7 mitigation — no implicit second rounds, no rolling changes.

Purpose: Put the site in its final pre-cutover state. After this plan, Plan 04 can flip DNS with confidence that the code is frozen.

Output: Updated source files reflecting revisions + perf fixes, a `revision-round.md` artifact documenting the round from kickoff to sign-off, and an updated (or unchanged, if Plan 02 passed) Lighthouse preview report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-launch-polish-handoff/04-RESEARCH.md
@.planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md
@.planning/phases/04-launch-polish-handoff/04-01-SUMMARY.md
@.planning/phases/04-launch-polish-handoff/04-02-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Revision round scope-control pattern from 04-RESEARCH Pitfall 7 (lines 376-391): -->
<!--   - Send explicit kickoff message with reply-by date -->
<!--   - Capture verbatim response in revision-round.md -->
<!--   - Items not on the list = deferred, written confirmation -->
<!--   - Single sign-off signal (approved email or checkbox) -->

<!-- Perf fix candidates from 04-RESEARCH Common Pitfalls 3-5 (if Plan 02 baseline < 93): -->
<!--   - Framer-motion above-the-fold: grep -rn "framer-motion\|motion\." src/ -->
<!--   - Hero video: poster + preload=metadata + mobile-static fallback -->
<!--   - next/image without sizes: grep -rn "next/image" src/ and audit each <Image> -->

<!-- AGENTS.md rule (Next.js 16 is not training data): consult node_modules/next/dist/docs/ before any Next-specific edits -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Kick off bounded revision round + capture client list</name>
  <what-built>Initiate the single bounded round of revisions. This is a checkpoint because it requires sending a message to the client, receiving a reply, and pasting that reply into a file. No code change in this task.</what-built>
  <how-to-verify>
    1. **Draft and send the revision-round kickoff message** (email or chat thread with the client). Required text patterns per 04-RESEARCH Pitfall 7:
       - Names the preview URL being reviewed (from Plan 02 artifacts).
       - States "this is the single revision round included in Milestone 1."
       - Provides an explicit reply-by date (target: 5 business days from send; do NOT leave open-ended).
       - Explicitly states: "Any requests received after [date] will be logged for Milestone 2 scope."
       - Asks the two open decision items from 04-RESEARCH Open Questions #2 and #4 if not already answered:
         - (a) apex vs www as canonical domain
         - (b) GitHub repo ownership transfer timing (Option A defer vs Option B transfer now)

    2. **Create `.planning/phases/04-launch-polish-handoff/artifacts/revision-round.md`** with this structure (populate as the round progresses):

       ```markdown
       # Revision Round — Milestone 1

       **Kickoff sent:** <YYYY-MM-DD>
       **Reply-by:** <YYYY-MM-DD>
       **Preview URL reviewed:** <URL>
       **Client contact:** Tammy / Skin Cafe

       ## Kickoff message (verbatim)
       <Paste the exact message sent>

       ## Client response (verbatim)
       <Paste or screenshot-reference the exact reply when received>

       ## Revision items
       | # | Request (client's words) | Implementation status | File(s) touched | Notes |
       |---|--------------------------|-----------------------|-----------------|-------|
       | 1 | ... | pending / implemented / deferred | ... | ... |

       ## Items deferred to Milestone 2
       | # | Request | Reason for deferral | Client acknowledged? |
       |---|---------|----------------------|----------------------|
       | ... | ... | ... | yes / no |

       ## Decisions captured
       - Canonical domain: <apex | www>
       - GitHub repo transfer: <Option A defer to post-handoff | Option B transfer in Plan 04>
       - Hero video mobile treatment: <keep as-is | poster-only on mobile | other> (only if raised)

       ## Sign-off
       <Client's final "approved" / "no further changes" message, verbatim or screenshot-referenced>
       **Sign-off date:** <YYYY-MM-DD>
       ```

    3. **Wait for client response.** When the response arrives, paste verbatim into the "Client response" section and populate the "Revision items" table.

    4. **Verify the doc is present before moving to Task 2:** `test -f .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md && grep -q "Client response" .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md`
  </how-to-verify>
  <resume-signal>Type "revision list captured" once the client's written response is pasted into the doc AND the revision items table is populated. If client response is overdue, escalate to Craig and reset the reply-by date; do not proceed to Task 2 with a blank list.</resume-signal>

  <files>N/A — checkpoint task, no file modification</files>
  <action>Human-action checkpoint. Intent: Initiate the single bounded round of revisions. This is a checkpoint because it requires sending a message to the client, receiving a reply, and pasting that reply into a file. No code change in this task. See the <what-built> and <how-to-verify> blocks above for the exact operational steps.</action>
  <verify><automated>echo 'checkpoint gate — see resume-signal for human verification'</automated></verify>
  <done>Checkpoint passed per resume-signal condition above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement perf fixes + revision items, update preview Lighthouse</name>
  <files>
    src/app/components/*.tsx
    src/app/**/*.tsx
    public/images/*
    .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md
    .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-post-revisions-mobile.html
  </files>
  <read_first>
    - `.planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md` — the "Feeds into Plan 03" section drives perf-fix scope. If that section says "no fixes needed," skip the perf subtasks entirely.
    - `.planning/phases/04-launch-polish-handoff/artifacts/revision-round.md` — the "Revision items" table drives the revision subtasks. If any item's intent is unclear from the client's words, surface as a sub-checkpoint before touching code; do NOT interpret.
    - AGENTS.md / CLAUDE.md: before editing any `.tsx` under `src/app/`, read the relevant doc in `node_modules/next/dist/docs/` for the API involved (e.g. `<Image>`, metadata exports, dynamic imports).
    - For framer-motion perf fixes: 04-RESEARCH Pitfall 3 (lines 317-329) — dynamic import with `ssr:false` pattern.
    - For hero video fixes: 04-RESEARCH Pitfall 4 (lines 331-345) — poster + preload=metadata.
    - For next/image sizes audit: 04-RESEARCH Pitfall 5 (lines 347-361).
  </read_first>
  <action>
    Execute in two sub-batches. Each subtask edits SPECIFIC files named in the input artifacts — no speculative refactors.

    **Sub-batch A — perf fixes (conditional on Plan 02 findings).**

    For each file-path + action entry in `responsive-qa-notes.md` "Feeds into Plan 03":
    1. Read the current file.
    2. Apply the exact change described. Examples mapped to 04-RESEARCH pitfalls:
       - **Framer-motion above-the-fold hit** → convert the component to dynamic import with `ssr:false`, OR replace the animation with CSS `@keyframes` (prefer CSS if the animation is decorative). Code pattern from 04-RESEARCH Pitfall 3: `const Motion = dynamic(() => import('framer-motion').then(m => m.motion.div), { ssr: false })`.
       - **Hero video weight** → ensure `<video>` has `poster="/images/hero-poster.jpg"` and `preload="metadata"` (not `auto`). If mobile variant is not acceptable at current weight, gate video behind `matchMedia('(min-width: 768px)')` and serve a poster-only `<img>` on mobile.
       - **next/image without `sizes`** → audit each `<Image>`, add `sizes` hint appropriate to the rendered width (e.g., `sizes="100vw"` for full-bleed hero, `sizes="(max-width: 768px) 100vw, 50vw"` for 2-col cards). Per 04-RESEARCH Pitfall 5.

    After each fix, re-run `npm run build` and confirm it still compiles.

    If the findings section says "no fixes needed" or the list is empty, skip sub-batch A entirely and record "No perf fixes required; preview baseline ≥ 93" in `revision-round.md` under a "Perf fixes" subsection.

    **Sub-batch B — revision-round items.**

    For each row in `revision-round.md` "Revision items" table with status `pending`:
    1. Read the client request verbatim.
    2. Determine the file(s) and specific change. If ambiguous, STOP and open a sub-checkpoint for clarification — do NOT guess.
    3. Apply the change.
    4. Update the row: status → `implemented`, populate `File(s) touched`.

    For any revision request that would materially change scope (e.g., "add a new booking provider", "add a CMS") — mark `deferred`, move to the "Items deferred to Milestone 2" table, and explicitly add to the client sign-off request that these are out-of-scope.

    **After sub-batches A + B:**
    1. Deploy to Vercel preview (push to a branch that triggers preview; Vercel auto-deploys per Phase 1 setup).
    2. Re-run mobile Lighthouse against the fresh preview URL and save as `lighthouse-post-revisions-mobile.html` in the artifacts directory. If Performance ≥ 93, we're green for Plan 04 cutover. If still < 93, either iterate once more on the biggest opportunity OR explicitly accept the gap in `revision-round.md` "Decisions captured" (client sign-off required on that acceptance).
    3. Re-run `npx playwright test` against the new preview URL to confirm no responsive regressions from the revision changes.

    Do NOT:
    - Implement any request not on the revision list (scope creep).
    - Allow the list to grow post-kickoff (Pitfall 7).
    - Skip re-running Lighthouse and Playwright after changes (regressions are trivially cheap to catch here and expensive to catch post-cutover).
  </action>
  <verify>
    <automated>
      # 1. Revision doc exists and all items have a status
      test -f .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md
      ! grep -E "^\| [0-9]+ \|.*\| pending \|" .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md
      # Expect no row still in `pending` state

      # 2. Post-revisions Lighthouse captured
      test -f .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-post-revisions-mobile.html
      test $(wc -c < .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-post-revisions-mobile.html) -gt 50000

      # 3. Build clean
      npm run build

      # 4. Playwright sweep passes against new preview
      PLAYWRIGHT_BASE_URL=<new-preview-url> npx playwright test
    </automated>
  </verify>
  <acceptance_criteria>
    - Every row in `revision-round.md` Revision items has status `implemented` or `deferred` (no `pending`).
    - Every `implemented` row names specific `File(s) touched`.
    - `lighthouse-post-revisions-mobile.html` exists, > 50KB, Performance score recorded in `revision-round.md`.
    - `npm run build` exits 0.
    - `npx playwright test` exits 0 against the post-revisions preview URL.
    - If Performance still < 93, `revision-round.md` "Decisions captured" section explicitly documents the accepted gap with client acknowledgment.
  </acceptance_criteria>
  <done>
    Revisions implemented or deferred with written disposition; perf fixes applied where Plan 02 identified them; preview is in the state we intend to flip DNS to.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Obtain client sign-off on revision round</name>
  <what-built>Final sign-off gate. Not a code change — a written "approved" confirmation from the client that closes Milestone 1's revision scope.</what-built>
  <how-to-verify>
    1. Send the client a sign-off message: "All revisions are live on [preview URL]. Please confirm by reply that these complete the Milestone 1 revision round. Any further changes will be scheduled for Milestone 2."
    2. Include the list of implemented items + the list of deferred items so the client sees the full disposition in one place.
    3. When the client replies with approval (any unambiguous "approved" / "looks good" / "ship it"), paste the reply into `revision-round.md` "Sign-off" section with the date.
    4. If the client requests more changes at this stage, this is scope creep — Craig decides: either (a) re-open the round once more with a tight reply-by, documented as an exception, or (b) accept the changes into Milestone 2 scope. Either path must be documented in the doc; do NOT silently roll the round.
  </how-to-verify>
  <resume-signal>Type "signed off" once the client's approval is captured in `revision-round.md` Sign-off section with a date. If client requests additional changes, document the path taken (re-open or defer) before resuming.</resume-signal>

  <files>N/A — checkpoint task, no file modification</files>
  <action>Human-action checkpoint. Intent: Final sign-off gate. Not a code change — a written "approved" confirmation from the client that closes Milestone 1's revision scope. See the <what-built> and <how-to-verify> blocks above for the exact operational steps.</action>
  <verify><automated>echo 'checkpoint gate — see resume-signal for human verification'</automated></verify>
  <done>Checkpoint passed per resume-signal condition above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → planner | Informal requests without specification; risk of misinterpretation |
| planner → production code | Changes driven by unstructured client input could introduce regressions |
| planner → Milestone 1 scope | Rolling/unbounded changes would break the $2,000 fixed-scope contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | Revision round scope | mitigate | Written kickoff with reply-by date (Task 1); verbatim client words captured; explicit sign-off signal (Task 3). Covers 04-RESEARCH Pitfall 7. |
| T-04-03-02 | Repudiation | Disputed revision-round completion | mitigate | `revision-round.md` captures: kickoff message, client response verbatim, per-item disposition, sign-off message. Full audit trail in git history. |
| T-04-03-03 | Information Disclosure | Client email/PII in revision-round.md | accept | Client communication expected in the doc. Redact real PII if any shows up (phone numbers, home addresses); client-business-email is acceptable. |
| T-04-03-04 | Denial of Service | Ambiguous client request blocks phase | mitigate | Task 2 action explicitly says: if a request is ambiguous, STOP and open sub-checkpoint for clarification rather than guess. Fails loudly, not silently. |
| T-04-03-05 | Elevation of Privilege | Client request inadvertently reduces security posture (e.g., "turn off spam protection") | mitigate | Any revision item that touches security surfaces (rate limits, CAPTCHA, form validation) must be flagged in the `revision-round.md` "Notes" column and confirmed with client before implementing. |
</threat_model>

<verification>
- `test -f .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md`
- `grep -q "Sign-off" .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md` returns a populated section (not just the heading)
- `! grep -Ei "\| pending \|" .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md` — no row remains pending
- Lighthouse post-revisions artifact exists and Performance ≥ 93 (or gap accepted in writing with client confirmation)
- `npm run build` + `npx playwright test` both exit 0 against post-revision preview
</verification>

<success_criteria>
- POLISH-04 delivered: exactly one round of revisions, captured from kickoff through sign-off in writing.
- POLISH-03 preview-URL target of ≥ 93 achieved (or gap accepted with client signature), providing safety margin for the production-URL deliverable in Plan 04.
- Client has explicitly acknowledged which items are in-scope vs Milestone 2.
- Repo state is frozen for Plan 04 cutover.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-polish-handoff/04-03-SUMMARY.md` with: the implemented count vs deferred count, final Lighthouse Performance score on preview, client sign-off date, and a one-line note on whether apex-vs-www + GitHub repo transfer decisions landed (these flow into Plan 04 task scope).
</output>
