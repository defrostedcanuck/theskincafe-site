---
phase: 04-launch-polish-handoff
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - HANDOFF.md
  - .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html
  - .planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md
  - .planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md
autonomous: false
requirements: [INFRA-04, INFRA-06, POLISH-03]
tags: [cutover, dns, ssl, handoff, github-transfer, production]
user_setup:
  - service: cloudflare
    why: "Pre-flip DNS record verification + last-minute edits if proxy state is wrong"
    dashboard_config:
      - task: "Verify apex A and www CNAME records show grey cloud (DNS only) — NOT orange cloud"
        location: "Cloudflare Dashboard → theskincafe.net zone → DNS → Records"
  - service: vercel
    why: "Attach production domain + configure apex/www redirect"
    dashboard_config:
      - task: "Add theskincafe.net + www.theskincafe.net as production domains; set primary = apex; configure Redirect on www → apex"
        location: "Vercel Dashboard (client team) → theskincafe-site project → Settings → Domains"
  - service: registrar-godaddy
    why: "Nameserver flip to Cloudflare NS pair. Per Phase 1 HANDOFF-CUTOVER-CHECKLIST Client-Blocking #2 registrar access: client-mediated or Craig-direct, depending on what was resolved in Plan 03."
    env_vars: []
    dashboard_config:
      - task: "Replace GoDaddy nameservers (ns41/42.domaincontrol.com) with Cloudflare-assigned nameserver pair (shown in Cloudflare zone overview)"
        location: "GoDaddy → Domain Portfolio → theskincafe.net → DNS → Nameservers → Change → Enter my own nameservers"
  - service: github
    why: "Repo transfer from defrostedcanuck/theskincafe-site to client's GitHub org (Option B from Phase 1 research, executed here if client has org)"
    dashboard_config:
      - task: "Initiate repo transfer to client-owned GitHub org; client accepts transfer"
        location: "GitHub → defrostedcanuck/theskincafe-site → Settings → Transfer ownership"
must_haves:
  truths:
    - "https://theskincafe.net returns 200 with server: Vercel header (not cloudflare)"
    - "http://theskincafe.net returns 308 (or 301) redirect to https://theskincafe.net"
    - "https://www.theskincafe.net returns 308 redirect to https://theskincafe.net (or vice versa, if client chose www primary)"
    - "dig theskincafe.net @1.1.1.1 resolves to 76.76.21.21 (Vercel apex), not a Cloudflare IP"
    - "Vercel project domain status shows 'Valid Configuration' and SSL 'Issued' with Let's Encrypt"
    - "HANDOFF.md exists at repo root covering: services matrix, credential locations, deploy flow, domain management, rotation runbooks, costs, support contact"
    - "Final production-URL mobile Lighthouse Performance ≥ 90 captured as HTML artifact and linked from HANDOFF.md"
    - "Phase 1 HANDOFF-CUTOVER-CHECKLIST rows are finalized (Status column updated with verified-by evidence)"
    - "GitHub repo is either transferred to client org (Option B) or explicitly deferred with a written path (Option A), decision captured in HANDOFF.md"
    - "Rollback plan for NS flip exists and is documented in cutover-log.md before the flip is performed"
    - "Cutover is NOT performed on a Friday afternoon (04-RESEARCH Pitfall 8)"
  artifacts:
    - path: "HANDOFF.md"
      provides: "Client-facing handoff document — every service, credential, rotation runbook, and cancellation procedure"
      min_lines: 200
      contains: "Services matrix"
    - path: ".planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html"
      provides: "Final POLISH-03 deliverable — Lighthouse mobile report against https://theskincafe.net post-cutover"
      min_lines: 1
    - path: ".planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md"
      provides: "Pre-flip checks, flip time, post-flip verification, rollback plan, any incidents"
      contains: "Rollback"
    - path: ".planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md"
      provides: "Finalized Phase 1 checklist with Status rows closed and verified-by references per T-01-03 threat from Phase 1"
      contains: "verified by"
  key_links:
    - from: "HANDOFF.md"
      to: ".planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html"
      via: "linked from Appendix"
      pattern: "lighthouse-production-mobile"
    - from: "cutover-log.md"
      to: "registrar rollback procedure"
      via: "documented NS values and 60s TTL window"
      pattern: "Rollback|ns41|domaincontrol"
    - from: "https://theskincafe.net"
      to: "Vercel project"
      via: "A record 76.76.21.21 + Cloudflare DNS-only"
      pattern: "76.76.21.21"
---

<objective>
Execute the serial, strictly-ordered cutover-and-handoff wave:
1. Verify pre-flip state (Cloudflare records DNS-only, CAA clean, records match Phase 1 baseline, feature-complete site on preview).
2. Attach production domain to Vercel project.
3. Flip nameservers at GoDaddy registrar.
4. Monitor propagation.
5. Wait for Vercel Let's Encrypt SSL provisioning.
6. Configure www↔apex redirect in Vercel dashboard.
7. Run final production-URL mobile Lighthouse as the POLISH-03 deliverable.
8. Author HANDOFF.md (INFRA-06).
9. Execute GitHub repo transfer (if client has org ready) or document deferred path.
10. Finalize Phase 1 HANDOFF-CUTOVER-CHECKLIST Status rows.
11. Obtain final client sign-off.

Purpose: Satisfy INFRA-04 (SSL cutover), INFRA-06 (handoff documentation), and the POLISH-03 production deliverable. This is the phase-closing plan — site becomes "live on client infrastructure" at the moment the NS flip resolves.

Output: Production domain serving HTTPS via Vercel-managed Let's Encrypt cert; HTTP→HTTPS + www→apex redirects active; comprehensive HANDOFF.md committed; final Lighthouse artifact; cutover log; finalized Phase 1 checklist; optional GitHub repo transfer.

**Scheduling constraint (locked):** Cutover execution window is Monday–Thursday morning Mesa/Phoenix time (Craig's timezone). Do NOT execute any task that touches DNS nameservers on a Friday afternoon or near a US federal holiday (04-RESEARCH Pitfall 8).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-launch-polish-handoff/04-RESEARCH.md
@.planning/phases/01-client-infrastructure-provisioning/01-RESEARCH.md
@.planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md
@.planning/phases/04-launch-polish-handoff/04-03-SUMMARY.md
@AGENTS.md

<interfaces>
<!-- Cutover sequence from 04-RESEARCH §Wave Ordering Pattern + 01-RESEARCH §DNS Cutover Sequence steps 7-10 (this phase's work): -->

Pre-flip verification (DO NOT SKIP):
  dig theskincafe.net @1.1.1.1 +short            # expect 76.76.21.21 (after flip)
  dig www.theskincafe.net @1.1.1.1 +short        # expect cname.vercel-dns.com + vercel IP
  dig CAA theskincafe.net @1.1.1.1 +short        # expect empty OR include letsencrypt.org
  dig NS theskincafe.net @8.8.8.8 +short         # pre-flip: GoDaddy ns41/42; post-flip: cloudflare NS

HTTP verification (post-flip):
  curl -sI http://theskincafe.net | head -5       # expect HTTP/2 308 + location: https://
  curl -sI https://theskincafe.net | head -5      # expect HTTP/2 200 + server: Vercel
  curl -sI https://www.theskincafe.net | head -5  # expect HTTP/2 308 + location: https://theskincafe.net/

Anti-pattern guard (MUST fail):
  curl -sI https://theskincafe.net | grep -i "cf-ray\|server: cloudflare"
  # If this returns ANY output, Cloudflare proxy is ON → STOP, fix, re-verify

<!-- HANDOFF.md structure from 04-RESEARCH §"Handoff Documentation Pattern" lines 206-253 (10 sections) -->

<!-- Vercel dashboard operations (no CLI equivalents for these per 04-RESEARCH): -->
  - Domains → Add Domain → theskincafe.net + www.theskincafe.net
  - Set Primary = theskincafe.net (apex)
  - Configure Redirect on www → theskincafe.net (per client decision from Plan 03)
  - Monitor SSL status: Pending → Issued (typically <60s, up to 15min)

<!-- Phase 1 HANDOFF-CUTOVER-CHECKLIST.md rows to finalize per Plan 01-01 T-01-03 threat: -->
  - Row 1 (Vercel team): verified by vercel teams ls output
  - Row 2 (Vercel billing): verified by team billing page screenshot
  - Rows 3-7: populate Status column + "verified by" evidence
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Schedule cutover window + confirm client availability + pre-flip readiness</name>
  <what-built>The go/no-go decision gate. Confirms scheduling constraints, registrar-access path, and that everything upstream is ready.</what-built>
  <how-to-verify>
    1. **Confirm scheduling window:** Proposed cutover day is a Monday, Tuesday, Wednesday, or Thursday morning Mesa/Phoenix time. No Fridays, no US federal holidays, no days when the Skin Cafe has a major event (confirm with Tammy — if a big booking day, defer). Document proposed date/time in `artifacts/cutover-log.md`.

    2. **Confirm registrar-access path** from Phase 1 HANDOFF-CUTOVER-CHECKLIST Open Question #2 and additional_context note: plan defaults to **client-mediated NS flip in GoDaddy UI**. Verify the client confirmed which path in Plan 03's revision round — either:
       - (a) Client executes the flip in GoDaddy while on a screenshare with Craig, OR
       - (b) Client has granted Craig GoDaddy login access.
       If neither is resolved, STOP and request client confirmation before proceeding. Record outcome in `cutover-log.md`.

    3. **Confirm Phase 3 is complete** (per 04-RESEARCH Assumption A3): Vercel preview URL serves the full feature set including Phase 2 (community + lead capture) and Phase 3 (SEO + analytics). Quick checks:
       - `curl -s https://<preview>/sitemap.xml | head -3` returns XML.
       - `curl -s https://<preview>/robots.txt | head -3` returns "User-agent".
       - Homepage source view includes OG meta tags.
       If any of these fail, STOP — cutover is premature.

    4. **Confirm Plan 03 sign-off landed:** `grep -q "Sign-off" .planning/phases/04-launch-polish-handoff/artifacts/revision-round.md` AND that section is populated with an actual date.

    5. **Confirm canonical domain decision:** Check `revision-round.md` "Decisions captured" section for apex-vs-www outcome. Default per 04-RESEARCH: apex. If client chose www, adjust Task 4's redirect direction accordingly.

    6. **Confirm GitHub transfer decision:** Same section of `revision-round.md`. Option A (defer) or Option B (transfer in Task 7). If no decision captured, default to Option A (per Phase 1 research recommendation) and document the deferral in HANDOFF.md.

    7. **Create `.planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md`** with this structure (fill pre-flip sections now):

       ```markdown
       # Cutover Log — Phase 4

       ## Go/No-Go decision
       - Proposed window: <YYYY-MM-DD HH:MM TZ>
       - Day-of-week check: <Mon/Tue/Wed/Thu, not holiday>
       - Registrar path: <client-mediated screenshare | Craig-direct login>
       - Canonical domain: <apex | www>
       - GitHub transfer: <Option A defer | Option B execute now>
       - Feature-complete verified: <date + evidence>
       - Plan 03 sign-off verified: <date>

       ## Rollback plan
       - Current GoDaddy nameservers (pre-flip): ns41.domaincontrol.com, ns42.domaincontrol.com
       - Cloudflare nameservers (to flip to): <ns-a, ns-b from Cloudflare zone overview>
       - Rollback trigger: HTTPS not live within 30 minutes of flip OR any client-reported mail/site outage
       - Rollback action: At GoDaddy → change NS back to ns41/42.domaincontrol.com → wait 60s (TTLs were lowered to 60s in Phase 1)
       - Rollback communication: Text Tammy with ETA estimate + reason

       ## Pre-flip verification (to be run just before Task 3)
       - [ ] dig apex resolves to pre-flip provider (mojohost or similar)
       - [ ] Cloudflare zone records: apex A = 76.76.21.21 (DNS only, grey cloud)
       - [ ] Cloudflare zone records: www CNAME = cname.vercel-dns.com (DNS only, grey cloud)
       - [ ] CAA records: empty OR include letsencrypt.org
       - [ ] All Phase 1 baseline records (MX for mail, other CNAMEs) present in Cloudflare
       - [ ] Vercel project has theskincafe.net + www.theskincafe.net added as pending domains
       - [ ] TTLs at GoDaddy = 60s (lowered in Phase 1 Plan 01-03)

       ## Flip event
       - Flip time: <HH:MM TZ>
       - Method: <client in GoDaddy UI | Craig in GoDaddy UI>

       ## Post-flip verification
       (populated in Task 3-4)

       ## Incidents
       (populated if any)
       ```
  </how-to-verify>
  <resume-signal>Type "go for cutover" once all seven checks pass and `cutover-log.md` pre-flip sections are populated. Type "defer" if any check fails; resume only when all are green.</resume-signal>

  <files>N/A — checkpoint task, no file modification</files>
  <action>Human-action checkpoint. Intent: The go/no-go decision gate. Confirms scheduling constraints, registrar-access path, and that everything upstream is ready. See the <what-built> and <how-to-verify> blocks above for the exact operational steps.</action>
  <verify><automated>echo 'checkpoint gate — see resume-signal for human verification'</automated></verify>
  <done>Checkpoint passed per resume-signal condition above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pre-flip verification + Vercel domain attach</name>
  <files>
    .planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md
  </files>
  <read_first>
    - 04-RESEARCH §"Code Examples > Cloudflare proxy status verification" (lines 530-539).
    - 04-RESEARCH §"Common Pitfalls 1 + 2" — Cloudflare proxy + CAA record causes of SSL stall.
    - 04-RESEARCH §"Vercel Domain Attach Pattern" (lines 170-178).
    - Phase 1 `HANDOFF-CUTOVER-CHECKLIST.md` — Post-Client-Action row 4 (Cloudflare zone) to confirm records are in place.
  </read_first>
  <action>
    Pre-flip verification is non-negotiable — run every check and mark in `cutover-log.md`. If any check fails, STOP, fix, re-verify. DO NOT proceed to Task 3 (NS flip) until every check passes.

    1. **Cloudflare records audit (DNS-only enforcement — 04-RESEARCH Pitfall 1):**
       - Via Cloudflare dashboard: confirm apex A record value `76.76.21.21`, Proxy status "DNS only" (grey cloud).
       - Confirm www CNAME value `cname.vercel-dns.com`, Proxy status "DNS only" (grey cloud).
       - If either shows orange cloud: toggle to DNS only; document in cutover-log.md as a pre-flip correction.
       - Command check (run BEFORE flip — resolves via current provider, so use Cloudflare public resolver to see Cloudflare's view): Not directly verifiable pre-flip via dig against 1.1.1.1 because the NS still points at GoDaddy. Instead, use Cloudflare's "Export" or screenshot the records table. Post-flip, `dig @1.1.1.1` will confirm.

    2. **CAA record check (04-RESEARCH Pitfall 2):**
       ```bash
       dig CAA theskincafe.net @8.8.8.8 +short
       ```
       - If empty: no action needed (Vercel auto-adds letsencrypt.org on SSL provisioning).
       - If present and does NOT include `letsencrypt.org`: add CAA record `0 issue "letsencrypt.org"` in Cloudflare before flip.

    3. **Phase 1 DNS baseline diff:** Read `.planning/phases/01-client-infrastructure-provisioning/artifacts/DNS-BASELINE.md` (referenced by the checklist). Confirm every non-web record (MX for mail, `mail.theskincafe.net` A, `_dmarc` TXT, any existing CNAMEs) is present in Cloudflare. If ANY are missing, add them before flip — per 04-RESEARCH Pitfall (Phase 1 research §Pitfall 1): missing MX = broken business email post-flip.

    4. **TTL verification at GoDaddy:** Login to GoDaddy DNS (client-mediated or direct per Task 1). Confirm all existing records show TTL = 60 seconds. If TTLs are higher (e.g., 3600), the 24h pre-flip wait window was not observed — STOP and wait 24h. Per 01-RESEARCH Pitfall 6.

    5. **Attach production domain to Vercel project:**
       - Via Vercel Dashboard (client team) → theskincafe-site project → Settings → Domains → Add Domain.
       - Add `theskincafe.net` — Vercel will show "Invalid Configuration" because NS still points at GoDaddy; this is expected. It will begin monitoring as soon as DNS resolves.
       - Add `www.theskincafe.net` the same way.
       - Set Primary = `theskincafe.net` (or `www` if Plan 03 captured that client preference).
       - Configure Redirect on the non-primary → primary.

    6. **Update cutover-log.md** pre-flip verification section with timestamps + evidence for each check. Example row: `- [x] CAA records: empty (verified 2026-04-24T14:30 via dig CAA theskincafe.net @8.8.8.8)`.
  </action>
  <verify>
    <automated>
      # 1. cutover-log.md populated pre-flip section
      grep -q "Pre-flip verification" .planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md
      grep -c "\[x\]" .planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md
      # Expect >= 5 checked items before proceeding to Task 3

      # 2. CAA record check
      dig CAA theskincafe.net @8.8.8.8 +short
      # Result: empty OR contains letsencrypt.org (manual interpretation required)

      # 3. Vercel domain attach visible in dashboard
      # Manual verification: Vercel Dashboard → Domains panel shows theskincafe.net + www.theskincafe.net
      # Both will show "Invalid Configuration" pre-flip — that's expected; they start validating post-flip
    </automated>
  </verify>
  <acceptance_criteria>
    - All items in `cutover-log.md` "Pre-flip verification" section are checked `[x]` with evidence.
    - Cloudflare apex A + www CNAME records both show grey cloud (DNS only) — confirmed visually in dashboard and documented.
    - CAA records either empty or include letsencrypt.org.
    - Phase 1 DNS baseline records (mail subdomain A, MX records if any, etc.) all present in Cloudflare.
    - Vercel project shows theskincafe.net + www.theskincafe.net in Domains list (status: Invalid Configuration, pending flip).
  </acceptance_criteria>
  <done>
    Every failure mode 04-RESEARCH warned about has been ruled out. Vercel is watching for the domain to resolve at it. Ready to flip.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Execute NS flip at GoDaddy + monitor propagation</name>
  <what-built>The flip itself — changes nameservers at GoDaddy from `ns41.domaincontrol.com` / `ns42.domaincontrol.com` to the Cloudflare-assigned NS pair. This step is explicitly client-action or client-mediated per the registrar-access decision from Task 1.</what-built>
  <how-to-verify>
    **Flip procedure:**
    1. **Note exact time of flip** in `cutover-log.md` "Flip event" section.
    2. At GoDaddy → Domain Portfolio → theskincafe.net → DNS → Nameservers → "Change" → "Enter my own nameservers (advanced)" → paste the two Cloudflare NS values (from Cloudflare zone overview; example format: `alex.ns.cloudflare.com`, `beth.ns.cloudflare.com` — actual values are specific to the zone).
    3. Save. GoDaddy confirms change.

    **Propagation monitoring (run from Craig's laptop):**
    ```bash
    # Poll every 30s for up to 30 minutes
    while true; do
      echo "=== $(date) ==="
      dig NS theskincafe.net @8.8.8.8 +short
      dig theskincafe.net @1.1.1.1 +short
      echo "---"
      sleep 30
    done
    ```
    - `dig NS` switches to Cloudflare NS values → NS flip propagated to Google DNS.
    - `dig theskincafe.net @1.1.1.1 +short` returns `76.76.21.21` → A record now served by Cloudflare.
    - Also verify via https://whatsmydns.net/#NS/theskincafe.net — global propagation map.

    **Non-regression check (email + subdomains — 01-RESEARCH Pitfall 1):**
    ```bash
    dig MX theskincafe.net @1.1.1.1 +short
    dig mail.theskincafe.net @1.1.1.1 +short
    # Confirm: MX records match pre-flip baseline; mail subdomain still resolves to mojohost IP
    ```

    **Rollback trigger:**
    - If at 30 minutes post-flip `dig theskincafe.net @1.1.1.1` still returns a non-Vercel IP OR the Vercel dashboard's domain status is still "Invalid Configuration", invoke rollback procedure from `cutover-log.md`:
      1. GoDaddy → Nameservers → change back to `ns41.domaincontrol.com` / `ns42.domaincontrol.com`.
      2. Wait 60s (TTLs are 60s).
      3. Re-verify original site responds.
      4. Text client, diagnose, schedule re-attempt.
    - Log rollback in `cutover-log.md` "Incidents" section with root cause.
  </how-to-verify>
  <resume-signal>Type "flipped" once `dig NS theskincafe.net @8.8.8.8 +short` returns Cloudflare nameservers AND `dig theskincafe.net @1.1.1.1 +short` returns `76.76.21.21` (both typically within 5-15 minutes). If rollback triggered, type "rolled back" and the phase pauses for diagnosis.</resume-signal>

  <files>N/A — checkpoint task, no file modification</files>
  <action>Human-action checkpoint. Intent: The flip itself — changes nameservers at GoDaddy from `ns41.domaincontrol.com` / `ns42.domaincontrol.com` to the Cloudflare-assigned NS pair. This step is explicitly client-action or client-mediated per the registrar-access decision from Task 1. See the <what-built> and <how-to-verify> blocks above for the exact operational steps.</action>
  <verify><automated>echo 'checkpoint gate — see resume-signal for human verification'</automated></verify>
  <done>Checkpoint passed per resume-signal condition above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Verify SSL provisioning + HTTP redirects + www/apex redirect</name>
  <files>
    .planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md
    .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html
  </files>
  <read_first>
    - 04-RESEARCH §"Code Examples > Post-cutover HTTP→HTTPS verification" (lines 512-526).
    - 04-RESEARCH §"Vercel SSL Provisioning Pattern" (lines 180-191) — Let's Encrypt HTTP-01 timing expectations (< 60s typical, up to 15min under rate limits).
    - 04-RESEARCH §"HTTP → HTTPS Redirect Pattern" — Vercel uses 308 (not 301); this satisfies ROADMAP criterion "HTTP 301s to HTTPS" per 04-RESEARCH Assumption A11.
  </read_first>
  <action>
    1. **Monitor SSL provisioning in Vercel dashboard.** Once NS flip propagates and Vercel sees the A record pointing at it, SSL status on both domains moves: `Pending` → `Issuing` → `Valid`. Typical < 60s. If > 15 min, suspect Cloudflare proxy (re-run Task 2 check 1) or CAA (re-run Task 2 check 2).

    2. **Post-flip HTTPS verification** (populate `cutover-log.md` "Post-flip verification"):
       ```bash
       # Apex serves 200 via Vercel
       curl -sI https://theskincafe.net | head -5
       # Expect: HTTP/2 200  + server: Vercel

       # HTTP redirects to HTTPS (Vercel automatic, status 308)
       curl -sI http://theskincafe.net | head -5
       # Expect: HTTP/2 308  + location: https://theskincafe.net/

       # www HTTPS redirects to apex (configured in Vercel dashboard)
       curl -sI https://www.theskincafe.net | head -5
       # Expect: HTTP/2 308  + location: https://theskincafe.net/

       # www HTTP redirects to apex HTTPS (chained: HTTP→HTTPS→apex, or single 308 direct)
       curl -sI http://www.theskincafe.net | head -5
       # Expect: final location https://theskincafe.net/

       # Anti-pattern guard: confirm Vercel is serving, NOT Cloudflare
       curl -sI https://theskincafe.net | grep -iE "server:|cf-ray|cloudflare"
       # Expect: server: Vercel. If cf-ray or server: cloudflare appears → STOP, proxy is on, go fix.

       # SSL cert issuer check
       echo | openssl s_client -connect theskincafe.net:443 -servername theskincafe.net 2>/dev/null | openssl x509 -issuer -noout
       # Expect: issuer=C = US, O = Let's Encrypt, CN = R<x> or E<x>
       ```

    3. **Mail sanity check:** Ask client to send a test email from a Skin Cafe email address and receive an external reply. Confirm mojohost MX records are still resolving (per Task 2 step 3 pre-check). Log in cutover-log.md.

    4. **Run final production Lighthouse (POLISH-03 deliverable):**
       ```bash
       npx lighthouse@13.1.0 https://theskincafe.net \
         --preset=mobile \
         --output=html \
         --output-path=.planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html \
         --chrome-flags="--headless"
       ```
       - Performance score MUST be ≥ 90 (ROADMAP success criterion #4).
       - If < 90: document delta vs preview baseline in cutover-log.md. If marginal (87-89), one-shot remediation: re-verify assets loading, clear Cloudflare cache if any stale record, retry. If systemic gap: surface to client as a post-launch fix item in Milestone 1.5 scope; do NOT hold the cutover open.

    5. **Playwright smoke against production:**
       ```bash
       PLAYWRIGHT_BASE_URL=https://theskincafe.net npx playwright test
       ```
       - All tests pass → green light.
       - Any failure → log in cutover-log.md, determine severity (layout break vs timing flake), fix if in-scope.

    6. **Update cutover-log.md** post-flip section: exact HTTP response headers from each curl, SSL issuer, Lighthouse score, Playwright result. Timestamp every check.
  </action>
  <verify>
    <automated>
      # 1. Production HTTPS 200
      curl -sI https://theskincafe.net | head -1 | grep -q "200"

      # 2. HTTP redirects
      curl -sI http://theskincafe.net | grep -iE "^location: https://theskincafe.net"

      # 3. www redirects to apex
      curl -sI https://www.theskincafe.net | grep -iE "^location: https://theskincafe.net"

      # 4. Vercel serving (not Cloudflare proxy)
      curl -sI https://theskincafe.net | grep -i "server:" | grep -qi "vercel"
      ! curl -sI https://theskincafe.net | grep -qi "cf-ray"

      # 5. SSL Let's Encrypt
      echo | openssl s_client -connect theskincafe.net:443 -servername theskincafe.net 2>/dev/null | openssl x509 -issuer -noout | grep -qi "Let's Encrypt"

      # 6. Lighthouse artifact exists
      test -f .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html
      test $(wc -c < .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html) -gt 50000

      # 7. Playwright against production passes
      PLAYWRIGHT_BASE_URL=https://theskincafe.net npx playwright test
    </automated>
  </verify>
  <acceptance_criteria>
    - `curl -sI https://theskincafe.net` returns HTTP/2 200 with `server: Vercel` header.
    - `curl -sI http://theskincafe.net` returns 308 with `location: https://theskincafe.net/`.
    - `curl -sI https://www.theskincafe.net` returns 308 with `location: https://theskincafe.net/` (or inverse, per canonical decision).
    - SSL issuer is Let's Encrypt.
    - NO Cloudflare proxy headers (`cf-ray`, `server: cloudflare`) on any response.
    - `lighthouse-production-mobile.html` exists, > 50KB, Performance ≥ 90 (or documented acceptance of marginal gap 87-89 with client ack).
    - Playwright responsive suite passes against production URL.
    - `cutover-log.md` "Post-flip verification" populated with timestamps + evidence.
  </acceptance_criteria>
  <done>
    Production domain is live on client infrastructure with valid SSL, automatic HTTP→HTTPS + www→apex redirects, passing Lighthouse, passing responsive tests. ROADMAP success criteria #1, #2, #3, #4 for Phase 4 all satisfied (#2 from Plan 01, #3 from Plan 02 + here, #4 here).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Author HANDOFF.md + finalize Phase 1 HANDOFF-CUTOVER-CHECKLIST</name>
  <files>
    HANDOFF.md
    .planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md
  </files>
  <read_first>
    - 04-RESEARCH §"Handoff Documentation Pattern" lines 206-253 — the 10-section template structure.
    - 04-RESEARCH §"Common Pitfalls" — re-read for rotation runbook content (Resend, Square, Vercel, Cloudflare).
    - Phase 1 HANDOFF-CUTOVER-CHECKLIST.md — the 7-row Client-Blocking table needs Status rows finalized per Phase 1 T-01-03 (verified-by evidence).
    - 01-RESEARCH §"Resend Reference Card", §"Square API Reference Card", §"Vercel Project Transfer Reference" for credential + rotation specifics.
    - `vercel env ls` output to enumerate current env vars (for HANDOFF §3 credential location map — names only, no values).
  </read_first>
  <action>
    **Part A — Author HANDOFF.md at repo root:**

    Follow the 10-section structure from 04-RESEARCH §"Handoff Documentation Pattern" exactly. Required sections and minimum content:

    1. **Executive summary** — one paragraph: what the site is, what it runs on (Next.js 16, Vercel, Cloudflare DNS, Resend transactional, Square for booking + customer directory), where the code lives (GitHub URL per Task 7 outcome), where the production URL is (https://theskincafe.net).

    2. **Services matrix** — table with columns: Service | Owner | Login URL | Role map (who has what role) | What it does | Monthly cost. Rows: Vercel, Cloudflare, Resend, Square Developer, Vercel Analytics (if enabled in Phase 3), GitHub.

    3. **Credential location map** — table listing every env var name, which scope it's in (Production/Preview/Development), and WHERE to find the value (NOT the value itself). Example row: `RESEND_API_KEY | Production, Preview | Vercel project → Settings → Environment Variables`. Security note inline per 04-RESEARCH line 258: "This document tells you where secrets live; it does not contain secrets themselves."

    4. **Deploy process** — numbered list: (1) push to `main` branch → Vercel auto-deploys; (2) PR to any branch → Vercel creates preview deploy at per-PR URL; (3) rollback = Vercel Dashboard → Deployments → pick previous → "Promote to Production"; (4) how to see build logs.

    5. **Domain management** — subsections: nameservers at GoDaddy point to Cloudflare NS pair `<list actual>`; DNS records edited in Cloudflare Dashboard; SSL is Vercel-managed Let's Encrypt, auto-renews, no action needed; emergency DNS procedure: "If Vercel is unreachable, create an A record at Cloudflare pointing apex to any working origin; TTL is 60s, rollback window is 60s".

    6. **Credential rotation runbooks** — step-by-step for each:
       - **Rotate Resend API key:** Resend dashboard → API Keys → generate new → update Vercel env `RESEND_API_KEY` → trigger Vercel redeploy → revoke old key.
       - **Rotate Square access token:** Square Developer Dashboard → Applications → <app> → Production Access Token → regenerate → update Vercel env `SQUARE_ACCESS_TOKEN` → redeploy → old token auto-invalidates.
       - **Rotate Vercel team member access:** Vercel team → Settings → Members → invite/remove.
       - **Rotate Cloudflare team member access:** Cloudflare → Manage Account → Members → invite/remove.
       - **Rotate GoDaddy registrar access:** GoDaddy → Account Settings → Delegate Access → add/remove.

    7. **Cost breakdown** — expected monthly: Vercel Hobby $0, Resend Free tier $0 (3k emails/mo), Cloudflare Free $0, Square API $0 (tied to existing merchant), GoDaddy domain renewal ~$20/yr. Scaling triggers: "If Vercel deployments exceed 100/month, upgrade to Pro $20/mo". Cancellation procedures per service.

    8. **Monitoring + what to watch** — Vercel Analytics dashboard (if Phase 3 added it), Resend bounce/complaint rates (stay < 5%), Cloudflare DNS query volume (informational).

    9. **Support** — Craig's contact: `craig@worry-free.it`. Response expectation: "best-effort, no SLA commitment. For urgent production issues, text me at <phone if client has it>." Each vendor's support URL: vercel.com/help, resend.com/support, etc. Per additional_context: do NOT commit Craig as 24/7 emergency contact; frame as best-effort + optional retainer offer.

    10. **Appendices** — links:
        - Final Lighthouse report: `.planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html`
        - Responsive QA notes: `.planning/phases/04-launch-polish-handoff/artifacts/responsive-qa-notes.md`
        - Revision round record: `.planning/phases/04-launch-polish-handoff/artifacts/revision-round.md`
        - Cutover log: `.planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md`
        - Phase 1 infrastructure baseline: `.planning/phases/01-client-infrastructure-provisioning/artifacts/DNS-BASELINE.md` and `HANDOFF-CUTOVER-CHECKLIST.md`.

    **Critical rule (per 04-RESEARCH line 257):** HANDOFF.md MUST NOT contain actual secrets. Only names + locations. Verify before commit: `grep -iE "re_[a-z0-9]+|sk_[a-z0-9]+|Bearer [A-Za-z0-9]" HANDOFF.md` returns nothing.

    **Part B — Finalize Phase 1 HANDOFF-CUTOVER-CHECKLIST.md:**

    Per Phase 1 T-01-03 threat model: every Client-Blocking row whose gate has cleared MUST have its Status column updated with verified-by evidence. For each of the 7 rows:

    1. Update the Status column from `⬜ Not started` to `✅ Complete` with date.
    2. Add a "Verified by" note inline or in a new column: the exact evidence (CLI output, dashboard screenshot reference, or file reference).

    Example for Row 7 (domain confirmation): `✅ 2026-04-25 — Verified by Plan 03 revision-round.md "Decisions captured": apex confirmed as canonical`.

    Append a closing block to the file:
    ```markdown
    ## Phase 4 Closure (2026-MM-DD)
    All Phase 1 Client-Blocking gates cleared. Phase 4 cutover executed YYYY-MM-DD. Production domain live on client infrastructure. See `../../04-launch-polish-handoff/artifacts/cutover-log.md` for details.
    ```
  </action>
  <verify>
    <automated>
      # 1. HANDOFF.md exists at repo root
      test -f HANDOFF.md
      test $(wc -l < HANDOFF.md) -gt 200

      # 2. All 10 sections present
      grep -c "^## " HANDOFF.md
      # Expect: >= 10

      # 3. No secrets leaked
      ! grep -iE "re_[a-z0-9]{10,}|sk_[a-z0-9]{10,}|Bearer [A-Za-z0-9]{20,}|xox[a-z]-[0-9]{10,}" HANDOFF.md

      # 4. Required services named
      grep -qi "vercel" HANDOFF.md
      grep -qi "cloudflare" HANDOFF.md
      grep -qi "resend" HANDOFF.md
      grep -qi "square" HANDOFF.md
      grep -qi "godaddy" HANDOFF.md
      grep -qi "github" HANDOFF.md

      # 5. Rotation runbooks present
      grep -qi "rotate.*resend\|rotate.*square" HANDOFF.md

      # 6. Craig contact present (but NOT as 24/7 emergency)
      grep -q "craig@worry-free" HANDOFF.md
      grep -qi "best-effort\|no SLA" HANDOFF.md

      # 7. Appendix links to all phase 4 artifacts
      grep -q "lighthouse-production-mobile" HANDOFF.md
      grep -q "revision-round" HANDOFF.md
      grep -q "cutover-log" HANDOFF.md

      # 8. Phase 1 checklist finalized
      grep -c "✅" .planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md
      # Expect: >= 7 (one per Client-Blocking row)
      grep -q "Phase 4 Closure" .planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `HANDOFF.md` exists at repo root, > 200 lines, contains all 10 sections.
    - Zero secrets leaked (secret-pattern grep returns empty).
    - All 5 vendor services + GitHub referenced by name.
    - Rotation runbooks present for Resend + Square + Vercel + Cloudflare + GoDaddy (at minimum).
    - Craig contact framed as best-effort, no SLA.
    - Appendix links to all Phase 4 artifacts.
    - Phase 1 HANDOFF-CUTOVER-CHECKLIST.md has ≥ 7 ✅ status markers and a "Phase 4 Closure" section.
  </acceptance_criteria>
  <done>
    Client has an authoritative, comprehensive handoff document at repo root. Phase 1 audit trail is closed. INFRA-06 satisfied.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 6: GitHub repo transfer decision point</name>
  <decision>Execute GitHub repo transfer from `defrostedcanuck/theskincafe-site` to client's GitHub org now (Option B), or defer indefinitely / to a post-handoff conversation (Option A — Phase 1 research default)?</decision>
  <context>
    - Phase 1 research recommended Option A (defer) as the Milestone 1 default. 04-RESEARCH additional_context surfaces this as an open question.
    - Outcome of Plan 03 should have captured the client's preference in `revision-round.md` "Decisions captured" section.
    - Option B requires: client has a GitHub org, client accepts transfer within ~7 days, Vercel GitHub App re-authorized on client org post-transfer.
    - Option A leaves repo at `defrostedcanuck`, adds client as collaborator if desired; documented as a deferred future task in HANDOFF.md.
  </context>
  <options>
    <option id="option-b">
      <name>Option B — Transfer now (execute Task 7)</name>
      <pros>Clean "client owns everything" end state. No outstanding deferrals.</pros>
      <cons>Brief Vercel auto-deploy interruption (minutes) while GitHub App re-authorizes. Requires client action to accept transfer. Git remote needs `git remote set-url` locally.</cons>
    </option>
    <option id="option-a">
      <name>Option A — Defer (skip Task 7, document in HANDOFF.md)</name>
      <pros>No disruption to deploy flow. No additional client action needed. Safe default.</pros>
      <cons>Technically the repo is still at Neural Rebel. If Craig's GitHub account becomes unavailable, client has no direct repo access. Mitigation: HANDOFF.md documents the deferral path and gives client the option to request transfer at any time.</cons>
    </option>
  </options>
  <resume-signal>Select: `option-a` or `option-b`. Update `HANDOFF.md` §2 Services Matrix → GitHub row + §10 Appendices with the chosen path, then resume to Task 7 (executes if Option B, no-op with doc update if Option A).</resume-signal>

  <files>N/A — checkpoint task, no file modification</files>
  <action>Decision checkpoint. Intent: Execute GitHub repo transfer from `defrostedcanuck/theskincafe-site` to client's GitHub org now (Option B), or defer indefinitely / to a post-handoff conversation (Option A — Phase 1 research default)? See the <options> block above for the choice set and the <resume-signal> for how to select.</action>
  <verify><automated>echo 'checkpoint gate — see resume-signal for human verification'</automated></verify>
  <done>Checkpoint passed per resume-signal condition above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Execute GitHub repo transfer OR document deferred path</name>
  <files>
    HANDOFF.md
    .planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md
  </files>
  <read_first>
    - 01-RESEARCH §"GitHub Repo Ownership" lines 370-383 — Option A vs Option B mechanics.
    - Task 6 decision outcome.
    - Current git remote: `git remote -v` from repo root.
  </read_first>
  <action>
    Branch on Task 6 outcome.

    **If Option B (transfer now):**
    1. **Confirm client has GitHub org + username to transfer to.** From Task 6 / Plan 03 decisions.
    2. **Initiate transfer via GitHub CLI:** `gh api -X POST /repos/defrostedcanuck/theskincafe-site/transfer -f new_owner=<client-org>` OR via web UI at github.com/defrostedcanuck/theskincafe-site/settings → Transfer ownership.
    3. **Wait for client to accept.** Transfer is a 2-step dance — initiator offers, recipient accepts. This may take minutes to days. If client is responsive, can be done live.
    4. **Post-transfer:**
       - Update local git remote: `git remote set-url origin git@github.com:<client-org>/theskincafe-site.git`.
       - Verify: `git remote -v` shows client org.
       - Vercel GitHub App: Vercel dashboard → Git Integration → may need to re-authorize on client org. Trigger one manual deploy: `vercel --prod` or via dashboard → confirm build kicks off.
       - Push a trivial commit (doc change — this task's updates qualify) to verify auto-deploy from client org.
    5. **Update HANDOFF.md** §2 Services Matrix: GitHub row Owner = client, Login URL = `github.com/<client-org>/theskincafe-site`, Role map = `<client admins> + craig@worry-free.it as collaborator`.
    6. **Log in `cutover-log.md`:** transfer initiated time, accepted time, Vercel re-auth status, first post-transfer deploy success.

    **If Option A (defer):**
    1. **No code operation.** Repo stays at `defrostedcanuck/theskincafe-site`.
    2. **Update HANDOFF.md** §2 Services Matrix: GitHub row Owner = Craig Richardson (`defrostedcanuck`), with a note "Repo ownership transfer deferred per client preference. Client may request transfer at any time; contact Craig."
    3. **Update HANDOFF.md §10** Appendices: add entry "GitHub repo transfer to client org — deferred (Option A). Trigger by emailing Craig with target org name; 24-hour turnaround."
    4. **Log in `cutover-log.md`:** "Option A — deferred. Client preference captured in Plan 03 revision-round.md."

    Do NOT:
    - Delete the repo at defrostedcanuck under any path.
    - Force-push to rewrite history.
    - Skip Vercel re-auth verification on Option B — silent deploy breakage is worse than no transfer.
  </action>
  <verify>
    <automated>
      # Branch verification on Task 6 outcome (check HANDOFF.md GitHub row for chosen path)

      # If Option B:
      # - git remote shows client org
      git remote -v | grep -qi "defrostedcanuck" && echo "Option A remainder" || echo "Option B executed"
      # - Trigger deploy completes
      # - HANDOFF.md lists client org as owner
      grep -A2 "^| GitHub" HANDOFF.md

      # If Option A:
      # - HANDOFF.md documents deferral path
      grep -qi "deferred\|Option A" HANDOFF.md
      grep -q "defrostedcanuck" HANDOFF.md

      # Either way: cutover-log.md documents the outcome
      grep -qi "github" .planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md
    </automated>
  </verify>
  <acceptance_criteria>
    - HANDOFF.md §2 GitHub row reflects actual current ownership (client org for B, `defrostedcanuck` for A).
    - cutover-log.md documents the Option A/B choice + outcome.
    - If Option B: `git remote -v` shows client org; one successful auto-deploy triggered from client org.
    - If Option A: explicit deferral language in HANDOFF.md with client-triggerable path.
  </acceptance_criteria>
  <done>
    GitHub ownership end-state matches the client's decision. Whichever path: it's documented, reproducible, and doesn't leave the repo in an ambiguous state.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8: Final client sign-off on Milestone 1 completion</name>
  <what-built>Entire Milestone 1: site live on client infrastructure at https://theskincafe.net, all 4 polish requirements delivered, handoff doc in repo, Phase 1 checklist closed, GitHub repo status clear.</what-built>
  <how-to-verify>
    Send the client a final sign-off email with these bullets:
    1. Production URL: https://theskincafe.net is live on your Vercel, your Cloudflare, your Resend, your Square.
    2. SSL: valid Let's Encrypt cert, auto-renews, no action needed from you.
    3. Lighthouse mobile Performance score: <actual score from Task 4>. Report at `<repo>/.planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html`.
    4. Responsive QA: passes at mobile/tablet/desktop widths.
    5. Revision round: all items on your list are implemented or deferred per your sign-off on <date>.
    6. Dark logo: live on scrolled navbar.
    7. Handoff doc: committed at repo root as HANDOFF.md — covers every service, credential, rotation procedure, cost, and support channel.
    8. GitHub repo: <Option A/B outcome from Task 7>.
    9. Phase 1 infrastructure checklist: closed. Every action row verified.

    Ask client to reply with acknowledgment. When received, update `cutover-log.md` with the sign-off date + quote, and update STATE.md `status` to `complete` for Phase 4.

    **If client reports an issue:** log in `cutover-log.md` "Incidents" section. Decide: in-scope (critical bug preventing launch acceptance → fix now, re-verify) vs out-of-scope (new feature request → Milestone 2). Do not silently implement out-of-scope requests.
  </how-to-verify>
  <resume-signal>Type "milestone complete" once client sign-off is recorded in `cutover-log.md`. If client reports blocking issues, type "issue reported" and resume only after they're resolved.</resume-signal>

  <files>N/A — checkpoint task, no file modification</files>
  <action>Human-action checkpoint. Intent: Entire Milestone 1: site live on client infrastructure at https://theskincafe.net, all 4 polish requirements delivered, handoff doc in repo, Phase 1 checklist closed, GitHub repo status clear. See the <what-built> and <how-to-verify> blocks above for the exact operational steps.</action>
  <verify><automated>echo 'checkpoint gate — see resume-signal for human verification'</automated></verify>
  <done>Checkpoint passed per resume-signal condition above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| registrar (GoDaddy) → DNS authority (Cloudflare) | NS flip moves authoritative DNS. Window of risk: 60s-15min of mixed resolution |
| Vercel edge → public internet | SSL cert issuance via Let's Encrypt HTTP-01; Cloudflare proxy interference is the #1 failure mode |
| Craig → client accounts | Admin access across 5 vendor dashboards; rotation/revocation must be cleanly documented |
| HANDOFF.md → public (if repo goes public) | Must not contain secrets, only pointers to secret locations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Denial of Service | DNS downtime during NS flip | mitigate | TTL lowered to 60s in Phase 1. Rollback plan in cutover-log.md Task 1 is a registrar NS revert. Scheduling constraint: no Fridays, no holidays (04-RESEARCH Pitfall 8). |
| T-04-04-02 | Denial of Service | SSL provisioning failure (Let's Encrypt HTTP-01 stall) | mitigate | Pre-flip verification (Task 2) confirms DNS-only (not proxied) + CAA clean. If SSL stalls > 15min post-flip, documented diagnostic path: re-check proxy status, re-check CAA, if both clean, contact Vercel support. Rollback path available within 30min window. |
| T-04-04-03 | Spoofing | Cloudflare proxy accidentally enabled post-handoff, breaking SSL renewal | mitigate | HANDOFF.md §5 Domain management explicitly warns: "Do NOT enable Cloudflare proxy (orange cloud) on theskincafe.net records — this breaks Vercel SSL and firewall." Training the client to leave it alone. |
| T-04-04-04 | Tampering | Unauthorized change to Cloudflare records post-handoff | mitigate | Cloudflare audit log captures all record changes with actor identity. Client owns the account with admin rights; Craig retains admin during handoff period. Rotation runbook §6 tells client how to revoke Craig's access when they want to. |
| T-04-04-05 | Information Disclosure | Secrets committed to HANDOFF.md | mitigate | Task 5 verify step: `grep -iE "re_|sk_|Bearer " HANDOFF.md` must return empty before commit. Architectural rule per 04-RESEARCH line 257-258. |
| T-04-04-06 | Repudiation | Disputed Milestone 1 completion | mitigate | cutover-log.md captures verify commands + timestamps + sign-off. HANDOFF.md committed with date. Phase 1 checklist finalized with verified-by evidence. Full git audit trail. |
| T-04-04-07 | Elevation of Privilege | Stale Craig access post-handoff | mitigate | HANDOFF.md §6 Rotation runbooks tell client exactly how to remove Craig from each vendor. No ongoing agency access required after Milestone 1 closes. |
| T-04-04-08 | Tampering | GitHub repo state during/after transfer | mitigate | Option B path includes post-transfer verification: remote URL check, Vercel re-auth, successful deploy. Option A path leaves repo under Craig with collaborator-level client access; transferable on demand. |
| T-04-04-09 | Denial of Service | Scope creep in final sign-off (Task 8) | mitigate | Task 8 action explicitly distinguishes in-scope (blocking bugs) vs out-of-scope (new features → Milestone 2). Written triage in cutover-log.md. |
| T-04-04-10 | Denial of Service | Credential rotation breaks production | accept | Runbooks in HANDOFF.md §6 spell out the correct sequence (generate new → update Vercel env → redeploy → revoke old). If client rotates out-of-sequence, runbook recovery is to re-run in correct order. Low-probability, self-recoverable. |
</threat_model>

<verification>
- `curl -sI https://theskincafe.net | head -1 | grep -q "200"` — site live
- `curl -sI http://theskincafe.net | grep -iE "^location: https://"` — HTTP→HTTPS
- `! curl -sI https://theskincafe.net | grep -qi "cf-ray\|server: cloudflare"` — no proxy
- SSL issuer is Let's Encrypt
- `test -f HANDOFF.md && test $(wc -l < HANDOFF.md) -gt 200`
- `! grep -iE "re_[a-z0-9]{10,}|sk_[a-z0-9]{10,}|Bearer [A-Za-z0-9]{20,}" HANDOFF.md` — no secrets
- `test -f .planning/phases/04-launch-polish-handoff/artifacts/lighthouse-production-mobile.html`
- `grep -q "Phase 4 Closure" .planning/phases/01-client-infrastructure-provisioning/artifacts/HANDOFF-CUTOVER-CHECKLIST.md`
- `grep -q "Sign-off" .planning/phases/04-launch-polish-handoff/artifacts/cutover-log.md` with populated date
- Final Lighthouse Performance ≥ 90 (or explicit documented gap acceptance with client ack)
</verification>

<success_criteria>
- INFRA-04 delivered: production domain serves HTTPS via Vercel-managed Let's Encrypt cert; HTTP→HTTPS is automatic.
- INFRA-06 delivered: HANDOFF.md at repo root covers every service, credential location, rotation runbook, cost, and support contact without leaking secrets.
- POLISH-03 final deliverable: Lighthouse mobile Performance ≥ 90 on production URL, HTML report committed.
- ROADMAP Phase 4 all 6 success criteria verifiable by grep + curl.
- Phase 1 HANDOFF-CUTOVER-CHECKLIST.md is closed with verified-by evidence on all Client-Blocking rows.
- GitHub repo end-state is documented and reproducible.
- Client has given written sign-off on Milestone 1.
</success_criteria>

<output>
After completion, create `.planning/phases/04-launch-polish-handoff/04-04-SUMMARY.md` capturing:
- Flip time (exact timestamp) and propagation duration.
- Final Lighthouse Performance score on production.
- SSL issuer + provisioning time.
- GitHub transfer outcome (A or B).
- Client sign-off date.
- Any incidents from cutover-log.md.

And update STATE.md:
- `status: complete` for Phase 4.
- `completed_phases: 4, total_phases: 4, percent: 100`.
- Move POLISH-01..04 + INFRA-04/06 to `Validated` status in REQUIREMENTS.md traceability table.
- Note in STATE.md that Milestone 1 is ready for `/gsd-complete-milestone`.
</output>
